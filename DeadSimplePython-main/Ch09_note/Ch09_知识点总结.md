# Chapter 09 - 集合与可迭代对象 (Collections and Iterables)

> **本章主题**: Python 的集合类型和迭代工具
> **面向**: 有 Java 开发经验的学习者

---

## 目录
1. [列表 (List)](#1-列表-list)
2. [元组 (Tuple)](#2-元组-tuple)
3. [字典 (Dictionary)](#3-字典-dictionary)
4. [集合 (Set)](#4-集合-set)
5. [双端队列 (Deque)](#5-双端队列-deque)
6. [命名元组 (NamedTuple)](#6-命名元组-namedtuple)
7. [序列解包 (Unpacking)](#7-序列解包-unpacking)
8. [切片 (Slicing)](#8-切片-slicing)
9. [迭代工具](#9-迭代工具)
10. [集合操作函数](#10-集合操作函数)
11. [模式匹配与集合](#11-模式匹配与集合)

---

## 1. 列表 (List)

### 1.1 列表基础

#### Python 实现
```python
# 创建列表
orders = [
    "caramel macchiato",
    "drip",
    "pumpkin spice latte",
    "cappuccino"
]

# 访问元素
print(orders[0])  # 第一个元素
print(orders[-1])  # 最后一个元素

# 修改元素
orders[1] = "americano"

# 添加元素
orders.append("espresso")  # 末尾添加

# 检查元素
if "drip" in orders:
    print("We have drip coffee")
```

#### Java 对比
```java
// Java - ArrayList
import java.util.ArrayList;
import java.util.List;

List<String> orders = new ArrayList<>(List.of(
    "caramel macchiato",
    "drip",
    "pumpkin spice latte",
    "cappuccino"
));

// 访问元素
System.out.println(orders.get(0));  // 第一个元素
System.out.println(orders.get(orders.size() - 1));  // 最后一个元素

// 修改元素
orders.set(1, "americano");

// 添加元素
orders.add("espresso");  // 末尾添加

// 检查元素
if (orders.contains("drip")) {
    System.out.println("We have drip coffee");
}
```

#### 🔑 核心差异
| 特性 | Python list | Java ArrayList |
|------|-------------|----------------|
| 索引方式 | `list[index]` | `list.get(index)` |
| 负数索引 | ✅ 支持 (`-1` 表示最后一个) | ❌ 不支持 |
| 修改元素 | `list[i] = value` | `list.set(i, value)` |
| 添加元素 | `append()`, `insert()` | `add()`, `add(index, value)` |
| 成员检查 | `in` 运算符 | `contains()` 方法 |
| 类型 | 可以混合类型 | 泛型约束单一类型 |

#### 💡 Javaer 注意事项
- ✅ Python 的 `list` ≈ Java 的 `ArrayList`
- ⚠️ **Python 支持负数索引**: `list[-1]` 表示最后一个元素,`list[-2]` 表示倒数第二个
- ⚠️ Python 列表可以包含不同类型的元素 (虽然不推荐)
- ✅ `in` 运算符 ≈ Java 的 `contains()` 方法
- 📌 记忆: "Python 列表更灵活,支持负数索引和混合类型"

**相关文件:** `print_list.py:1-4`, `in_orders.py:1-15`

### 1.2 for-else 循环

#### Python 实现
```python
orders = []

# for-else: else 块在循环正常结束时执行
for order in orders:
    print(order)
else:
    print("(No orders)")  # 列表为空时执行

# 如果使用 break,else 不会执行
for order in orders:
    if order == "done":
        break  # break 会跳过 else
else:
    print("All orders processed")  # 正常结束才执行
```

#### Java 对比
Java 没有直接对应的 `for-else` 语法:

```java
// Java - 使用标志变量模拟
List<String> orders = new ArrayList<>();
boolean hasOrders = false;

for (String order : orders) {
    System.out.println(order);
    hasOrders = true;
}

if (!hasOrders) {
    System.out.println("(No orders)");
}

// 使用 break 的情况
boolean brokeEarly = false;
for (String order : orders) {
    if (order.equals("done")) {
        brokeEarly = true;
        break;
    }
}

if (!brokeEarly) {
    System.out.println("All orders processed");
}
```

#### 🔑 核心差异
| 特性 | Python for-else | Java |
|------|-----------------|------|
| else 块 | ✅ 原生支持 | ❌ 不支持 |
| 语义 | else 在循环正常结束时执行 | 需要标志变量 |
| break 影响 | break 会跳过 else | 手动检查标志 |
| 代码简洁性 | 高 | 低 (需要额外变量) |

#### 💡 Javaer 注意事项
- ⚠️ **Python 独有的 for-else 语法**,Java 没有对应概念
- ✅ `else` 块只在循环**正常结束**时执行 (没有 break)
- ⚠️ 如果使用 `break` 退出循环,`else` 块不会执行
- 📌 记忆: "for-else 的 else 表示'如果循环正常结束'"

**相关文件:** `print_list.py:1-6`

### 1.3 列表操作

#### Python 实现
```python
# 获取列表长度
orders = ["latte", "mocha", "espresso"]
print(len(orders))  # 3

# 排序
customers = [
    ('Newman', 'tea'),
    ('Daniel', 'lemongrass tea'),
    ('Jason', 'pumpkin spice latte')
]

# 按第二个元素排序 (饮料名称)
for name, drink in sorted(customers, key=lambda x: x[1]):
    print(drink)

# 列表聚合
orders_per_day = [56, 41, 49, 22, 71, 43, 18]
total = sum(orders_per_day)
average = sum(orders_per_day) // len(orders_per_day)
print(f"Average: {average}")
```

#### Java 对比
```java
// 获取列表长度
List<String> orders = List.of("latte", "mocha", "espresso");
System.out.println(orders.size());  // 3

// 排序
List<Pair<String, String>> customers = List.of(
    new Pair<>("Newman", "tea"),
    new Pair<>("Daniel", "lemongrass tea"),
    new Pair<>("Jason", "pumpkin spice latte")
);

// 按第二个元素排序
customers.stream()
    .sorted(Comparator.comparing(Pair::getValue))
    .forEach(p -> System.out.println(p.getValue()));

// 列表聚合
List<Integer> ordersPerDay = List.of(56, 41, 49, 22, 71, 43, 18);
int total = ordersPerDay.stream().mapToInt(Integer::intValue).sum();
int average = total / ordersPerDay.size();
System.out.println("Average: " + average);
```

#### 🔑 核心差异
| 特性 | Python | Java |
|------|--------|------|
| 长度 | `len(list)` | `list.size()` |
| 排序 | `sorted(list, key=...)` | `stream().sorted(...)` |
| 聚合函数 | `sum()`, `max()`, `min()` | `stream().sum()`, `max()`, `min()` |
| 内置函数 | 丰富的内置函数 | 依赖 Stream API |

#### 💡 Javaer 注意事项
- ✅ `len(list)` ≈ Java 的 `list.size()`
- ⚠️ Python 的 `len()` 是全局函数,Java 的 `size()` 是方法
- ✅ Python 的 `sorted()` ≈ Java 的 `stream().sorted()`
- ✅ Python 的 `sum()` ≈ Java 的 `stream().sum()`
- 📌 记忆: "Python 内置函数更多,Java 依赖 Stream API"

**相关文件:** `len_customers.py:1-2`, `sort_orders.py:1-16`, `average_orders.py:1-3`

---

## 2. 元组 (Tuple)

### 2.1 元组基础

#### Python 实现
```python
# 创建元组
order = ("cappuccino", "medium", 4.95)

# 访问元素 (和列表相同)
drink = order[0]
size = order[1]
price = order[2]

# 元组是不可变的
# order[0] = "latte"  # TypeError: 'tuple' object does not support item assignment

# 元组拆包
drink, size, price = order
print(f"{drink} ({size}): ${price}")
```

#### Java 对比
Java 没有内置的元组类型,通常用自定义类或记录类:

```java
// Java - 使用记录类 (Java 14+)
record Order(String drink, String size, double price) {}

// 创建
Order order = new Order("cappuccino", "medium", 4.95);

// 访问元素
String drink = order.drink();
String size = order.size();
double price = order.price();

// 记录类是不可变的 (final 字段)
// order.drink = "latte";  // 编译错误

// Java 没有元组拆包,需要逐一赋值
String drink = order.drink();
String size = order.size();
double price = order.price();
```

#### 🔑 核心差异
| 特性 | Python tuple | Java Record |
|------|--------------|-------------|
| 定义 | `(a, b, c)` | `record Name(Type a, Type b)` |
| 不可变性 | ✅ 不可变 | ✅ 不可变 (final) |
| 索引访问 | `tuple[0]` | `record.field()` |
| 拆包 | ✅ 支持 | ❌ 不支持 |
| 类型声明 | 不需要 | 需要 |
| 轻量级 | 极轻量 | 需要定义类 |

#### 💡 Javaer 注意事项
- ✅ Python 的 `tuple` ≈ Java 的 `Record` 类 (Java 14+)
- ⚠️ 元组是轻量级的不可变序列,不需要定义类
- ✅ 元组支持拆包: `a, b, c = tuple`
- ⚠️ 元组可以包含不同类型的元素
- 📌 记忆: "元组 = 不可变列表 + 拆包功能"

**相关文件:** `order_tuple.py:1-5`

---

## 3. 字典 (Dictionary)

### 3.1 字典基础

#### Python 实现
```python
# 创建字典
menu = {
    'drip': 2.95,
    'cappuccino': 4.95,
    'americano': 3.45,
    'latte': 4.95,
    'espresso': 2.95
}

# 访问元素
print(menu['latte'])  # 4.95

# 添加/修改元素
menu['mocha'] = 5.45

# 删除元素
del menu['drip']

# 检查键是否存在
if 'latte' in menu:
    print(f"Latte costs ${menu['latte']}")

# 安全访问 (EAFP 风格)
try:
    price = menu['chai']
except KeyError:
    price = 0.0

# 安全访问 (LBYL 风格)
if 'chai' in menu:
    price = menu['chai']
else:
    price = 0.0

# 使用 get() 方法 (推荐)
price = menu.get('chai', 0.0)  # 如果键不存在,返回默认值
```

#### Java 对比
```java
// Java - HashMap
import java.util.HashMap;
import java.util.Map;

// 创建字典
Map<String, Double> menu = new HashMap<>(Map.of(
    "drip", 2.95,
    "cappuccino", 4.95,
    "americano", 3.45,
    "latte", 4.95,
    "espresso", 2.95
));

// 访问元素
System.out.println(menu.get("latte"));  // 4.95

// 添加/修改元素
menu.put("mocha", 5.45);

// 删除元素
menu.remove("drip");

// 检查键是否存在
if (menu.containsKey("latte")) {
    System.out.println("Latte costs $" + menu.get("latte"));
}

// 安全访问 (异常处理)
Double price;
try {
    price = menu.get("chai");
    if (price == null) throw new NullPointerException();
} catch (NullPointerException e) {
    price = 0.0;
}

// 安全访问 (检查后访问)
if (menu.containsKey("chai")) {
    price = menu.get("chai");
} else {
    price = 0.0;
}

// 使用 getOrDefault() 方法 (推荐)
price = menu.getOrDefault("chai", 0.0);
```

#### 🔑 核心差异
| 特性 | Python dict | Java HashMap |
|------|-------------|--------------|
| 访问语法 | `dict[key]` | `map.get(key)` |
| 键不存在 | 抛出 `KeyError` | 返回 `null` |
| 成员检查 | `in` 运算符 | `containsKey()` |
| 默认值 | `get(key, default)` | `getOrDefault(key, default)` |
| 删除 | `del dict[key]` | `map.remove(key)` |
| 有序性 | ✅ 有序 (Python 3.7+) | ❌ 无序 (HashMap) |

#### 💡 Javaer 注意事项
- ✅ Python 的 `dict` ≈ Java 的 `HashMap`
- ⚠️ **Python 字典从 3.7+ 保证插入顺序**,Java HashMap 无序
- ⚠️ Python 字典访问不存在的键会抛出 `KeyError`,Java 返回 `null`
- ✅ 使用 `get(key, default)` 避免异常 ≈ Java 的 `getOrDefault()`
- 📌 记忆: "Python 字典有序且抛异常,Java HashMap 无序且返回 null"

**相关文件:** `menu_dict.py:1-10`, `checkout_dict_eafp.py`, `checkout_dict_lbyl.py`

### 3.2 字典迭代

#### Python 实现
```python
menu = {
    'drip': 2.95,
    'cappuccino': 4.95,
    'latte': 4.95
}

# 迭代键
for drink in menu:
    print(drink)

# 迭代值
for price in menu.values():
    print(f"${price}")

# 迭代键值对
for drink, price in menu.items():
    print(f"{drink}: ${price}")

# 迭代键 (显式)
for drink in menu.keys():
    print(drink)
```

#### Java 对比
```java
Map<String, Double> menu = Map.of(
    "drip", 2.95,
    "cappuccino", 4.95,
    "latte", 4.95
);

// 迭代键
for (String drink : menu.keySet()) {
    System.out.println(drink);
}

// 迭代值
for (Double price : menu.values()) {
    System.out.println("$" + price);
}

// 迭代键值对
for (Map.Entry<String, Double> entry : menu.entrySet()) {
    System.out.println(entry.getKey() + ": $" + entry.getValue());
}

// Java 8+ forEach
menu.forEach((drink, price) ->
    System.out.println(drink + ": $" + price)
);
```

#### 🔑 核心差异
| 特性 | Python | Java |
|------|--------|------|
| 迭代键 | `for k in dict:` | `for k : map.keySet()` |
| 迭代值 | `for v in dict.values():` | `for v : map.values()` |
| 迭代键值对 | `for k, v in dict.items():` | `for entry : map.entrySet()` |
| 拆包 | ✅ 自动拆包 | ❌ 需要 `entry.getKey()` |
| Lambda | `forEach` 不常用 | `forEach` 常用 |

#### 💡 Javaer 注意事项
- ✅ Python 的 `dict.items()` ≈ Java 的 `map.entrySet()`
- ⚠️ **Python 可以自动拆包键值对**: `for k, v in dict.items()`
- ⚠️ Python 默认迭代键,Java 需要显式调用 `keySet()`
- 📌 记忆: "Python 字典迭代更简洁,支持自动拆包"

**相关文件:** `iterate_orders_dict.py:1-15`

---

## 4. 集合 (Set)

### 4.1 集合基础

#### Python 实现
```python
# 创建集合
raffle = {'Kyle', 'Denis', 'Jason'}

# 添加元素
raffle.add('Newman')

# 删除元素
raffle.discard('Denis')  # 如果元素不存在,不会报错
raffle.remove('Kyle')    # 如果元素不存在,抛出 KeyError

# 弹出随机元素
winner = raffle.pop()

# 检查成员
if 'Jason' in raffle:
    print("Jason is in the raffle")

# 集合运算
prev_winners = frozenset({'Denis', 'Simon'})
raffle -= prev_winners  # 移除之前的获奖者
```

#### Java 对比
```java
// Java - HashSet
import java.util.HashSet;
import java.util.Set;

// 创建集合
Set<String> raffle = new HashSet<>(Set.of("Kyle", "Denis", "Jason"));

// 添加元素
raffle.add("Newman");

// 删除元素
raffle.remove("Denis");  // 返回 boolean,不抛异常

// 弹出元素 (Java 没有直接方法,需要迭代器)
String winner = null;
if (!raffle.isEmpty()) {
    winner = raffle.iterator().next();
    raffle.remove(winner);
}

// 检查成员
if (raffle.contains("Jason")) {
    System.out.println("Jason is in the raffle");
}

// 集合运算
Set<String> prevWinners = Set.of("Denis", "Simon");
raffle.removeAll(prevWinners);  // 移除之前的获奖者
```

#### 🔑 核心差异
| 特性 | Python set | Java HashSet |
|------|------------|--------------|
| 创建 | `{a, b, c}` | `new HashSet<>(Set.of(...))` |
| 添加 | `add()` | `add()` |
| 删除 | `remove()`, `discard()` | `remove()` |
| 弹出 | `pop()` | 需要迭代器 |
| 成员检查 | `in` | `contains()` |
| 集合运算 | `-`, `|`, `&`, `^` | `removeAll()`, `addAll()`, `retainAll()` |

#### 💡 Javaer 注意事项
- ✅ Python 的 `set` ≈ Java 的 `HashSet`
- ⚠️ Python 集合支持运算符: `-` (差集), `|` (并集), `&` (交集), `^` (对称差集)
- ⚠️ `discard()` 不会抛异常,`remove()` 会抛异常
- ⚠️ Python 的 `pop()` 弹出随机元素,Java 需要用迭代器
- 📌 记忆: "Python 集合运算更简洁,支持运算符"

**相关文件:** `raffle_set.py:1-9`

### 4.2 frozenset (不可变集合)

#### Python 实现
```python
# 创建不可变集合
prev_winners = frozenset({'Denis', 'Simon'})

# frozenset 不能修改
# prev_winners.add('Jason')  # AttributeError

# 可以用于集合运算
raffle = {'Kyle', 'Denis', 'Jason'}
raffle -= prev_winners  # 移除之前的获奖者

# frozenset 可以作为字典的键 (set 不行)
winner_counts = {
    frozenset({'Denis', 'Jason'}): 2,
    frozenset({'Kyle'}): 1
}
```

#### Java 对比
```java
// Java - 不可变集合
Set<String> prevWinners = Set.of("Denis", "Simon");  // 不可变

// 不可修改
// prevWinners.add("Jason");  // UnsupportedOperationException

// 可以用于集合运算
Set<String> raffle = new HashSet<>(Set.of("Kyle", "Denis", "Jason"));
raffle.removeAll(prevWinners);

// Java 的不可变集合不能作为 Map 的键 (因为 equals/hashCode)
// 但可以使用 List 或自定义不可变类
```

#### 🔑 核心差异
| 特性 | Python frozenset | Java Set.of() |
|------|------------------|---------------|
| 不可变性 | ✅ 完全不可变 | ✅ 完全不可变 |
| 可哈希 | ✅ 可以作为 dict 键 | ⚠️ 不常作为 Map 键 |
| 创建 | `frozenset({...})` | `Set.of(...)` |
| 集合运算 | ✅ 支持 | ⚠️ 需要转换为可变集合 |

#### 💡 Javaer 注意事项
- ✅ Python 的 `frozenset` ≈ Java 的 `Set.of()` (不可变集合)
- ⚠️ `frozenset` 可以作为字典键,因为它是可哈希的
- ⚠️ 普通 `set` 不能作为字典键,因为可变对象不可哈希
- 📌 记忆: "frozenset = 不可变 + 可哈希的 set"

**相关文件:** `raffle_frozenset.py:1-6`

---

## 5. 双端队列 (Deque)

### 5.1 双端队列基础

#### Python 实现
```python
from collections import deque

# 创建双端队列
customers = deque(['Kyle', 'Jason', 'Simon'])

# 从右侧添加
customers.append('Newman')

# 从左侧添加
customers.appendleft('Daniel')

# 从右侧移除
last = customers.pop()

# 从左侧移除
first = customers.popleft()

print(customers)  # deque(['Kyle', 'Jason', 'Simon'])
```

#### Java 对比
```java
// Java - LinkedList (实现 Deque 接口)
import java.util.LinkedList;
import java.util.Deque;

// 创建双端队列
Deque<String> customers = new LinkedList<>(List.of("Kyle", "Jason", "Simon"));

// 从右侧添加
customers.addLast("Newman");  // 或 add()

// 从左侧添加
customers.addFirst("Daniel");

// 从右侧移除
String last = customers.removeLast();  // 或 poll()

// 从左侧移除
String first = customers.removeFirst();  // 或 pollFirst()

System.out.println(customers);  // [Kyle, Jason, Simon]
```

#### 🔑 核心差异
| 特性 | Python deque | Java LinkedList/Deque |
|------|--------------|----------------------|
| 导入 | `from collections import deque` | `java.util.Deque` |
| 右侧添加 | `append()` | `addLast()` 或 `add()` |
| 左侧添加 | `appendleft()` | `addFirst()` |
| 右侧移除 | `pop()` | `removeLast()` 或 `poll()` |
| 左侧移除 | `popleft()` | `removeFirst()` 或 `pollFirst()` |
| 性能 | O(1) 两端操作 | O(1) 两端操作 |

#### 💡 Javaer 注意事项
- ✅ Python 的 `deque` ≈ Java 的 `LinkedList` (实现 Deque 接口)
- ⚠️ Python 的 `deque` 专为两端快速操作设计
- ✅ 两端添加/删除都是 O(1) 时间复杂度
- 📌 记忆: "deque = double-ended queue,两端都快"

**相关文件:** `customers_deque.py:1-9`

---

## 6. 命名元组 (NamedTuple)

### 6.1 命名元组基础

#### Python 实现
```python
from collections import namedtuple

# 定义命名元组
CoffeeOrder = namedtuple('CoffeeOrder', ['customer', 'drink', 'size'])

# 创建实例
order = CoffeeOrder('Jason', 'pumpkin spice latte', 'venti')

# 访问字段 (三种方式)
print(order.customer)  # 通过名称访问
print(order[0])        # 通过索引访问
print(order.drink)     # 通过名称访问

# 拆包
customer, drink, size = order
print(f"{customer} ordered {drink} ({size})")

# 不可变
# order.customer = 'Kyle'  # AttributeError
```

#### Java 对比
```java
// Java - Record 类 (Java 14+)
record CoffeeOrder(String customer, String drink, String size) {}

// 创建实例
CoffeeOrder order = new CoffeeOrder("Jason", "pumpkin spice latte", "venti");

// 访问字段
System.out.println(order.customer());  // 通过方法访问
// 无法通过索引访问

// Java 没有元组拆包
String customer = order.customer();
String drink = order.drink();
String size = order.size();
System.out.println(customer + " ordered " + drink + " (" + size + ")");

// 不可变
// order.customer = "Kyle";  // 编译错误 (final 字段)
```

#### 🔑 核心差异
| 特性 | Python namedtuple | Java Record |
|------|-------------------|-------------|
| 定义 | `namedtuple('Name', [fields])` | `record Name(types)` |
| 访问方式 | 名称和索引 | 仅名称 (方法) |
| 拆包 | ✅ 支持 | ❌ 不支持 |
| 不可变性 | ✅ 不可变 | ✅ 不可变 |
| 轻量级 | 极轻量 | 需要声明类型 |

#### 💡 Javaer 注意事项
- ✅ Python 的 `namedtuple` ≈ Java 的 `Record` 类
- ⚠️ `namedtuple` 既可以通过名称访问,也可以通过索引访问
- ✅ `namedtuple` 支持拆包: `a, b, c = named_tuple`
- ⚠️ `namedtuple` 是不可变的,像元组一样
- 📌 记忆: "namedtuple = 有名字的元组 = 轻量级的 Record"

**相关文件:** `coffeeorder_namedtuple.py:1-8`

---

## 7. 序列解包 (Unpacking)

### 7.1 基本解包

#### Python 实现
```python
# 元组解包
order = ("cappuccino", "medium", 4.95)
drink, size, price = order

# 列表解包
customers = ['Kyle', 'Jason', 'Simon']
first, second, third = customers

# 交换变量 (非常优雅!)
a, b = 1, 2
a, b = b, a  # 交换
print(a, b)  # 2 1
```

#### Java 对比
```java
// Java 没有元组解包,需要逐一赋值
class Order {
    String drink, size;
    double price;
}

Order order = new Order("cappuccino", "medium", 4.95);
String drink = order.drink;
String size = order.size;
double price = order.price;

// 列表解包
List<String> customers = List.of("Kyle", "Jason", "Simon");
String first = customers.get(0);
String second = customers.get(1);
String third = customers.get(2);

// 交换变量 (需要临时变量)
int a = 1, b = 2;
int temp = a;
a = b;
b = temp;
System.out.println(a + " " + b);  // 2 1
```

#### 🔑 核心差异
| 特性 | Python | Java |
|------|--------|------|
| 元组解包 | `a, b, c = tuple` | ❌ 不支持 |
| 列表解包 | `a, b, c = list` | ❌ 不支持 |
| 交换变量 | `a, b = b, a` | 需要临时变量 |
| 语法简洁性 | 极简 | 冗长 |

#### 💡 Javaer 注意事项
- ⚠️ **Python 独有的解包语法**,Java 没有对应功能
- ✅ 解包让代码更简洁: `a, b, c = values`
- ⚠️ 交换变量不需要临时变量: `a, b = b, a`
- 📌 记忆: "Python 解包 = 同时赋值多个变量"

### 7.2 扩展解包 (*rest)

#### Python 实现
```python
# * 运算符收集剩余元素
customers = ['Kyle', 'Jason', 'Simon', 'Denis', 'Newman']

# 取第一个,剩余的收集到列表
first, *rest = customers
print(first)  # 'Kyle'
print(rest)   # ['Jason', 'Simon', 'Denis', 'Newman']

# 取第一个和最后一个,中间的收集到列表
first, *middle, last = customers
print(first)   # 'Kyle'
print(middle)  # ['Jason', 'Simon', 'Denis']
print(last)    # 'Newman'

# 取前两个,剩余的收集
first, second, *rest = customers
print(first)   # 'Kyle'
print(second)  # 'Jason'
print(rest)    # ['Simon', 'Denis', 'Newman']

# 忽略某些元素
first, _, *rest = customers  # 忽略第二个元素
```

#### Java 对比
Java 没有直接的解包语法,需要手动处理:

```java
// Java - 手动分割列表
List<String> customers = List.of("Kyle", "Jason", "Simon", "Denis", "Newman");

// 取第一个,剩余的作为子列表
String first = customers.get(0);
List<String> rest = customers.subList(1, customers.size());
System.out.println(first);  // Kyle
System.out.println(rest);   // [Jason, Simon, Denis, Newman]

// 取第一个和最后一个,中间的作为子列表
String firstCustomer = customers.get(0);
String lastCustomer = customers.get(customers.size() - 1);
List<String> middle = customers.subList(1, customers.size() - 1);

// 取前两个,剩余的作为子列表
String first2 = customers.get(0);
String second = customers.get(1);
List<String> rest2 = customers.subList(2, customers.size());
```

#### 🔑 核心差异
| 特性 | Python *rest | Java |
|------|--------------|------|
| 扩展解包 | `first, *rest = list` | ❌ 不支持 |
| 中间元素 | `first, *mid, last = list` | 需要 subList |
| 语法 | 极简 | 冗长 |
| 灵活性 | 高 | 低 |

#### 💡 Javaer 注意事项
- ⚠️ **Python 独有的 `*` 扩展解包**,Java 没有对应功能
- ✅ `*rest` 会收集剩余的所有元素到列表
- ⚠️ 一个解包表达式中只能有一个 `*`
- 📌 记忆: "* 表示'剩余的所有元素'"

**相关文件:** `unpack_customers.py:1-6`, `unpack_menu.py:1-2`

---

## 8. 切片 (Slicing)

### 8.1 基本切片

#### Python 实现
```python
orders = [
    "caramel macchiato",
    "drip",
    "pumpkin spice latte",
    "cappuccino",
    "americano",
    "espresso",
    "mocha"
]

# 基本切片: [start:stop]
print(orders[1:4])     # ['drip', 'pumpkin spice latte', 'cappuccino']
print(orders[2:5])     # ['pumpkin spice latte', 'cappuccino', 'americano']

# 从头开始: [:stop]
print(orders[:3])      # ['caramel macchiato', 'drip', 'pumpkin spice latte']

# 到末尾: [start:]
print(orders[4:])      # ['americano', 'espresso', 'mocha']

# 负数索引
print(orders[-3:])     # ['americano', 'espresso', 'mocha'] (最后3个)
print(orders[:-2])     # 除了最后2个的所有元素

# 完整拷贝
copy = orders[:]       # 浅拷贝整个列表
```

#### Java 对比
```java
List<String> orders = List.of(
    "caramel macchiato",
    "drip",
    "pumpkin spice latte",
    "cappuccino",
    "americano",
    "espresso",
    "mocha"
);

// 基本切片: subList(start, end)
System.out.println(orders.subList(1, 4));  // [drip, pumpkin spice latte, cappuccino]
System.out.println(orders.subList(2, 5));  // [pumpkin spice latte, cappuccino, americano]

// 从头开始
System.out.println(orders.subList(0, 3));  // 前3个

// 到末尾
System.out.println(orders.subList(4, orders.size()));  // 从索引4到末尾

// 负数索引 (Java 不支持,需要计算)
System.out.println(orders.subList(orders.size() - 3, orders.size()));  // 最后3个
System.out.println(orders.subList(0, orders.size() - 2));  // 除了最后2个

// 完整拷贝
List<String> copy = new ArrayList<>(orders);
```

#### 🔑 核心差异
| 特性 | Python | Java |
|------|--------|------|
| 切片语法 | `list[start:stop]` | `list.subList(start, end)` |
| 负数索引 | ✅ `list[-1]`, `list[-3:]` | ❌ 需要计算 |
| 省略参数 | `[:3]`, `[2:]` | 需要 `0` 或 `size()` |
| 步长 | `[::2]` | ❌ 不支持 |
| 拷贝 | `list[:]` | `new ArrayList<>(list)` |

#### 💡 Javaer 注意事项
- ✅ Python 的 `list[1:4]` ≈ Java 的 `list.subList(1, 4)`
- ⚠️ **Python 切片更简洁**,支持省略参数和负数索引
- ⚠️ Python 切片不包含结束索引: `[1:4]` 返回索引 1, 2, 3
- ⚠️ Python 的 `list[:]` 创建浅拷贝
- 📌 记忆: "切片 = [start:stop:step],灵活省略参数"

**相关文件:** `slice_orders.py:1-23`

### 8.2 步长切片

#### Python 实现
```python
orders = ["a", "b", "c", "d", "e", "f", "g", "h"]

# 带步长: [start:stop:step]
print(orders[::2])     # ['a', 'c', 'e', 'g'] (每隔一个)
print(orders[1::2])    # ['b', 'd', 'f', 'h'] (从索引1开始,每隔一个)
print(orders[::3])     # ['a', 'd', 'g'] (每隔两个)

# 反向切片: step = -1
print(orders[::-1])    # 反转列表
print(orders[-1::-1])  # 从最后一个开始反转
print(orders[:0:-1])   # 从末尾到索引1 (不包括0)

# 复杂切片
print(orders[1:6:2])   # ['b', 'd', 'f'] (索引1到6,步长2)
```

#### Java 对比
Java 没有内置的步长切片,需要手动实现:

```java
List<String> orders = List.of("a", "b", "c", "d", "e", "f", "g", "h");

// 每隔一个元素 (手动实现)
List<String> every2 = new ArrayList<>();
for (int i = 0; i < orders.size(); i += 2) {
    every2.add(orders.get(i));
}
System.out.println(every2);  // [a, c, e, g]

// 反转列表
List<String> reversed = new ArrayList<>(orders);
Collections.reverse(reversed);
System.out.println(reversed);

// Java 8+ Stream 方式
List<String> every2Stream = IntStream.range(0, orders.size())
    .filter(i -> i % 2 == 0)
    .mapToObj(orders::get)
    .collect(Collectors.toList());
```

#### 🔑 核心差异
| 特性 | Python | Java |
|------|--------|------|
| 步长语法 | `[::step]` | ❌ 不支持 |
| 反转 | `[::-1]` | `Collections.reverse()` |
| 每隔 N 个 | `[::N]` | 手动循环或 Stream |
| 语法简洁性 | 极简 | 冗长 |

#### 💡 Javaer 注意事项
- ⚠️ **Python 独有的步长切片**,Java 没有对应语法
- ✅ `list[::-1]` 反转列表,非常优雅
- ⚠️ `list[::2]` 取偶数索引元素
- 📌 记忆: "步长 = 每隔多少个元素取一个"

**相关文件:** `slice_orders.py:14-23`

---

## 9. 迭代工具

### 9.1 enumerate() - 带索引迭代

#### Python 实现
```python
customers = ['Kyle', 'Jason', 'Simon', 'Denis']

# 使用 enumerate() 同时获取索引和值
for index, customer in enumerate(customers):
    print(f"#{index + 1}: {customer}")

# 输出:
# #1: Kyle
# #2: Jason
# #3: Simon
# #4: Denis

# 指定起始索引
for index, customer in enumerate(customers, start=1):
    print(f"#{index}: {customer}")
```

#### Java 对比
```java
List<String> customers = List.of("Kyle", "Jason", "Simon", "Denis");

// 传统 for 循环
for (int i = 0; i < customers.size(); i++) {
    System.out.println("#" + (i + 1) + ": " + customers.get(i));
}

// Java 8+ Stream (需要手动跟踪索引)
IntStream.range(0, customers.size())
    .forEach(i -> System.out.println("#" + (i + 1) + ": " + customers.get(i)));

// 或使用第三方库 (如 Guava)
// Streams.forEachWithIndex(...)
```

#### 🔑 核心差异
| 特性 | Python enumerate | Java |
|------|------------------|------|
| 语法 | `for i, v in enumerate(list):` | `for (int i = 0; ...)` |
| 起始索引 | `enumerate(list, start=1)` | 手动调整 |
| 简洁性 | 极简 | 冗长 |
| 返回值 | 元组 `(index, value)` | - |

#### 💡 Javaer 注意事项
- ⚠️ Python 的 `enumerate()` 简化了带索引的迭代
- ✅ `enumerate(list, start=1)` 可以指定起始索引
- ⚠️ `enumerate()` 返回 `(index, value)` 元组,可以自动解包
- 📌 记忆: "enumerate = 枚举 = 带编号的迭代"

**相关文件:** `enumerate_orders.py:1-5`

### 9.2 zip() - 并行迭代

#### Python 实现
```python
customers = ['Kyle', 'Jason', 'Simon']
usuals = ['pumpkin spice latte', 'americano', 'tea']

# 并行迭代两个列表
for customer, drink in zip(customers, usuals):
    print(f"{customer} usually orders {drink}")

# 输出:
# Kyle usually orders pumpkin spice latte
# Jason usually orders americano
# Simon usually orders tea

# 多个列表
sizes = ['venti', 'grande', 'tall']
for customer, drink, size in zip(customers, usuals, sizes):
    print(f"{customer}: {size} {drink}")
```

#### Java 对比
```java
List<String> customers = List.of("Kyle", "Jason", "Simon");
List<String> usuals = List.of("pumpkin spice latte", "americano", "tea");

// 传统方式 (手动索引)
for (int i = 0; i < Math.min(customers.size(), usuals.size()); i++) {
    System.out.println(customers.get(i) + " usually orders " + usuals.get(i));
}

// Java 8+ Stream (使用 IntStream)
IntStream.range(0, Math.min(customers.size(), usuals.size()))
    .forEach(i -> System.out.println(
        customers.get(i) + " usually orders " + usuals.get(i)
    ));

// 多个列表
List<String> sizes = List.of("venti", "grande", "tall");
IntStream.range(0, Math.min(customers.size(), Math.min(usuals.size(), sizes.size())))
    .forEach(i -> System.out.println(
        customers.get(i) + ": " + sizes.get(i) + " " + usuals.get(i)
    ));
```

#### 🔑 核心差异
| 特性 | Python zip | Java |
|------|-----------|------|
| 语法 | `for a, b in zip(list1, list2):` | 手动索引 |
| 多个列表 | `zip(a, b, c, ...)` | 需要嵌套 min |
| 长度不一致 | 以最短的为准 | 需要手动 min |
| 简洁性 | 极简 | 冗长 |

#### 💡 Javaer 注意事项
- ⚠️ Python 的 `zip()` 极大简化了并行迭代
- ✅ `zip()` 可以接受任意数量的可迭代对象
- ⚠️ 如果列表长度不一致,`zip()` 以最短的为准
- 📌 记忆: "zip = 拉链 = 将多个列表合并迭代"

**相关文件:** `usuals_zip.py:1-9`

### 9.3 map() - 映射函数

#### Python 实现
```python
# 简单映射
orders = ['latte', 'mocha', 'espresso']
upper_orders = map(str.upper, orders)
print(list(upper_orders))  # ['LATTE', 'MOCHA', 'ESPRESSO']

# 使用 lambda
prices = [4.95, 5.45, 2.95]
with_tax = map(lambda p: p * 1.08, prices)
print(list(with_tax))  # [5.346, 5.886, 3.186]

# 多个列表
from operator import add
cost = [5.95, 4.95, 5.45]
tip = [0.25, 1.00, 2.00]
total = map(add, cost, tip)
for t in total:
    print(f"{t:.02f}")
```

#### Java 对比
```java
// 简单映射
List<String> orders = List.of("latte", "mocha", "espresso");
List<String> upperOrders = orders.stream()
    .map(String::toUpperCase)
    .collect(Collectors.toList());
System.out.println(upperOrders);  // [LATTE, MOCHA, ESPRESSO]

// 使用 lambda
List<Double> prices = List.of(4.95, 5.45, 2.95);
List<Double> withTax = prices.stream()
    .map(p -> p * 1.08)
    .collect(Collectors.toList());
System.out.println(withTax);  // [5.346, 5.886, 3.186]

// 多个列表 (需要索引)
List<Double> cost = List.of(5.95, 4.95, 5.45);
List<Double> tip = List.of(0.25, 1.00, 2.00);
IntStream.range(0, Math.min(cost.size(), tip.size()))
    .mapToDouble(i -> cost.get(i) + tip.get(i))
    .forEach(t -> System.out.printf("%.02f%n", t));
```

#### 🔑 核心差异
| 特性 | Python map | Java Stream.map |
|------|-----------|-----------------|
| 语法 | `map(func, list)` | `stream().map(func)` |
| 返回值 | 迭代器 | Stream |
| 多个列表 | `map(func, list1, list2)` | 需要索引 |
| 惰性求值 | ✅ 惰性 | ✅ 惰性 |

#### 💡 Javaer 注意事项
- ✅ Python 的 `map()` ≈ Java 的 `stream().map()`
- ⚠️ `map()` 返回迭代器,需要 `list()` 转换为列表
- ⚠️ `map()` 可以接受多个列表,对应元素一起传给函数
- 📌 记忆: "map = 映射 = 对每个元素应用函数"

**相关文件:** `brew_map.py:1-3`, `grand_total_map.py:1-7`

### 9.4 filter() - 过滤元素

#### Python 实现
```python
orders = [
    "caramel macchiato",
    "drip",
    "pumpkin spice latte",
    "cappuccino",
    "espresso"
]

# 过滤出包含 "latte" 的订单
latte_orders = filter(lambda x: "latte" in x, orders)
print(list(latte_orders))  # ['pumpkin spice latte']

# 过滤出价格大于4的饮料
menu = {
    'drip': 2.95,
    'cappuccino': 4.95,
    'latte': 4.95,
    'espresso': 2.95
}

expensive = filter(lambda item: item[1] > 4, menu.items())
for drink, price in expensive:
    print(f"{drink}: ${price}")
```

#### Java 对比
```java
List<String> orders = List.of(
    "caramel macchiato",
    "drip",
    "pumpkin spice latte",
    "cappuccino",
    "espresso"
);

// 过滤出包含 "latte" 的订单
List<String> latteOrders = orders.stream()
    .filter(x -> x.contains("latte"))
    .collect(Collectors.toList());
System.out.println(latteOrders);  // [pumpkin spice latte]

// 过滤出价格大于4的饮料
Map<String, Double> menu = Map.of(
    "drip", 2.95,
    "cappuccino", 4.95,
    "latte", 4.95,
    "espresso", 2.95
);

menu.entrySet().stream()
    .filter(entry -> entry.getValue() > 4)
    .forEach(entry -> System.out.println(
        entry.getKey() + ": $" + entry.getValue()
    ));
```

#### 🔑 核心差异
| 特性 | Python filter | Java Stream.filter |
|------|--------------|-------------------|
| 语法 | `filter(predicate, list)` | `stream().filter(predicate)` |
| 返回值 | 迭代器 | Stream |
| 惰性求值 | ✅ 惰性 | ✅ 惰性 |

#### 💡 Javaer 注意事项
- ✅ Python 的 `filter()` ≈ Java 的 `stream().filter()`
- ⚠️ `filter()` 返回迭代器,需要 `list()` 转换
- ⚠️ `filter()` 接受谓词函数,返回 True 的元素会保留
- 📌 记忆: "filter = 过滤 = 只保留满足条件的元素"

**相关文件:** `orders_filter.py:1-7`

---

## 10. 集合操作函数

### 10.1 通用操作函数

#### Python 实现
```python
orders = ["latte", "mocha", "espresso", "cappuccino"]

# 长度
print(len(orders))  # 4

# 求和 (数字列表)
prices = [4.95, 5.45, 2.95, 4.95]
print(sum(prices))  # 18.30

# 最大值和最小值
print(max(prices))  # 5.45
print(min(prices))  # 2.95

# 排序
sorted_orders = sorted(orders)  # 返回新列表
orders.sort()  # 原地排序

# 反转
reversed_orders = list(reversed(orders))  # 返回迭代器
orders.reverse()  # 原地反转

# 成员检查
if "latte" in orders:
    print("We have latte")
```

#### Java 对比
```java
List<String> orders = new ArrayList<>(List.of("latte", "mocha", "espresso", "cappuccino"));

// 长度
System.out.println(orders.size());  // 4

// 求和 (数字列表)
List<Double> prices = List.of(4.95, 5.45, 2.95, 4.95);
double sum = prices.stream().mapToDouble(Double::doubleValue).sum();
System.out.println(sum);  // 18.30

// 最大值和最小值
double max = prices.stream().mapToDouble(Double::doubleValue).max().orElse(0);
double min = prices.stream().mapToDouble(Double::doubleValue).min().orElse(0);

// 排序
List<String> sortedOrders = new ArrayList<>(orders);
Collections.sort(sortedOrders);  // 返回新列表
orders.sort(Comparator.naturalOrder());  // 原地排序

// 反转
List<String> reversedOrders = new ArrayList<>(orders);
Collections.reverse(reversedOrders);
Collections.reverse(orders);  // 原地反转

// 成员检查
if (orders.contains("latte")) {
    System.out.println("We have latte");
}
```

#### 🔑 核心差异
| 特性 | Python | Java |
|------|--------|------|
| 长度 | `len(list)` | `list.size()` |
| 求和 | `sum(list)` | `stream().sum()` |
| 最大/最小 | `max()`, `min()` | `stream().max()`, `min()` |
| 排序 | `sorted()`, `list.sort()` | `Collections.sort()`, `list.sort()` |
| 反转 | `reversed()`, `list.reverse()` | `Collections.reverse()` |
| 成员检查 | `in` | `contains()` |

#### 💡 Javaer 注意事项
- ✅ Python 的内置函数更简洁: `len()`, `sum()`, `max()`, `min()`
- ⚠️ `sorted()` 返回新列表,`list.sort()` 原地排序
- ⚠️ `reversed()` 返回迭代器,需要 `list()` 转换
- 📌 记忆: "Python 内置函数丰富,Java 依赖方法和 Stream"

**相关文件:** `len_customers.py:1-2`, `sort_orders.py:1-16`, `average_orders.py:1-3`

---

## 11. 模式匹配与集合

### 11.1 序列模式匹配

#### Python 实现
```python
order = ['mocha', 'cappuccino', 'latte', 'espresso']

match order:
    case []:
        print("Empty order")
    case [drink]:
        print(f"Single drink: {drink}")
    case [drink1, drink2]:
        print(f"Two drinks: {drink1} and {drink2}")
    case [first, *rest]:
        print(f"First: {first}, Rest: {rest}")
    case _:
        print("Multiple drinks")
```

#### Java 对比
Java 的 `switch` 不支持序列模式匹配,需要 if-else:

```java
List<String> order = List.of("mocha", "cappuccino", "latte", "espresso");

if (order.isEmpty()) {
    System.out.println("Empty order");
} else if (order.size() == 1) {
    System.out.println("Single drink: " + order.get(0));
} else if (order.size() == 2) {
    System.out.println("Two drinks: " + order.get(0) + " and " + order.get(1));
} else {
    String first = order.get(0);
    List<String> rest = order.subList(1, order.size());
    System.out.println("First: " + first + ", Rest: " + rest);
}
```

#### 🔑 核心差异
| 特性 | Python match | Java switch |
|------|--------------|-------------|
| 序列匹配 | ✅ `case [a, b]:` | ❌ 不支持 |
| 通配符 | `case [first, *rest]:` | ❌ 不支持 |
| 模式结构 | 强大 | 基础 |

#### 💡 Javaer 注意事项
- ⚠️ **Python 的模式匹配支持序列结构**,Java 不支持
- ✅ 可以匹配空列表: `case []:`
- ✅ 可以匹配固定长度: `case [a, b]:`
- ✅ 可以使用 `*` 收集剩余元素: `case [first, *rest]:`
- 📌 记忆: "Python match 可以匹配序列结构"

**相关文件:** `match_coffee_sequence.py:1-10`

### 11.2 字典模式匹配

#### Python 实现
```python
order = {
    'size': 'venti',
    'notes': 'no whip',
    'drink': 'mocha latte',
    'serve': 'for here'
}

match order:
    case {'size': 'tall', 'serve': 'for here', **rest}:
        drink = f"{rest['notes']} {rest['drink']}"
        print(f"Filling ceramic mug with {drink}.")
    case {'size': 'grande', 'serve': 'to go', **rest}:
        drink = f"{rest['notes']} {rest['drink']}"
        print(f"Filling large paper cup with {drink}.")
    case {'size': 'venti', 'serve': 'for here', **rest}:
        drink = f"{rest['notes']} {rest['drink']}"
        print(f"Filling extra large tumbler with {drink}.")
```

#### Java 对比
Java 不支持字典模式匹配:

```java
Map<String, String> order = Map.of(
    "size", "venti",
    "notes", "no whip",
    "drink", "mocha latte",
    "serve", "for here"
);

String size = order.get("size");
String serve = order.get("serve");

if ("tall".equals(size) && "for here".equals(serve)) {
    String drink = order.get("notes") + " " + order.get("drink");
    System.out.println("Filling ceramic mug with " + drink + ".");
} else if ("grande".equals(size) && "to go".equals(serve)) {
    String drink = order.get("notes") + " " + order.get("drink");
    System.out.println("Filling large paper cup with " + drink + ".");
} else if ("venti".equals(size) && "for here".equals(serve)) {
    String drink = order.get("notes") + " " + order.get("drink");
    System.out.println("Filling extra large tumbler with " + drink + ".");
}
```

#### 🔑 核心差异
| 特性 | Python match | Java |
|------|--------------|------|
| 字典匹配 | ✅ `case {'key': value}:` | ❌ 不支持 |
| 剩余键值对 | `**rest` | 手动过滤 |
| 部分匹配 | ✅ 只需匹配部分键 | 手动检查 |

#### 💡 Javaer 注意事项
- ⚠️ **Python 的模式匹配支持字典结构**,Java 不支持
- ✅ 可以匹配部分键: `case {'size': 'tall', **rest}:`
- ✅ `**rest` 收集剩余的键值对
- 📌 记忆: "Python match 可以匹配字典结构"

**相关文件:** `match_coffee_dictionary.py:1-17`

---

## 12. 总结对比表

### Python vs Java 集合类型

| 特性 | Python | Java |
|------|--------|------|
| **列表** | `list` (动态数组) | `ArrayList` |
| **元组** | `tuple` (不可变) | Record 类 |
| **字典** | `dict` (有序) | `HashMap` (无序) |
| **集合** | `set`, `frozenset` | `HashSet`, `Set.of()` |
| **双端队列** | `deque` | `LinkedList`/`Deque` |
| **命名元组** | `namedtuple` | Record 类 |
| **负数索引** | ✅ 支持 | ❌ 不支持 |
| **切片** | ✅ `[start:stop:step]` | `subList()` (无步长) |
| **解包** | ✅ `a, b = tuple` | ❌ 不支持 |
| **for-else** | ✅ 独有 | ❌ 不支持 |
| **enumerate** | `enumerate(list)` | 手动索引 |
| **zip** | `zip(list1, list2)` | 手动索引 |
| **map** | `map(func, list)` | `stream().map()` |
| **filter** | `filter(pred, list)` | `stream().filter()` |
| **模式匹配** | ✅ 序列和字典 | ❌ 不支持 |

---

## 13. 学习路径建议

1. **掌握基础集合** - list, tuple, dict, set
2. **理解可变性** - 可变 vs 不可变集合
3. **学习切片和索引** - 负数索引,步长切片
4. **掌握解包** - 基本解包,扩展解包 `*rest`
5. **使用迭代工具** - enumerate, zip, map, filter
6. **实践模式匹配** - 序列和字典的模式匹配
7. **对比 Java** - 建立心智模型

---

## 📊 参考文件索引

| 主题 | 文件名 |
|------|--------|
| 列表 | `print_list.py`, `in_orders.py`, `len_customers.py`, `sort_orders.py` |
| 元组 | `order_tuple.py` |
| 字典 | `menu_dict.py`, `iterate_orders_dict.py`, `checkout_dict_eafp.py`, `checkout_dict_lbyl.py` |
| 集合 | `raffle_set.py`, `raffle_frozenset.py` |
| 双端队列 | `customers_deque.py` |
| 命名元组 | `coffeeorder_namedtuple.py` |
| 解包 | `unpack_customers.py`, `unpack_menu.py` |
| 切片 | `slice_orders.py` |
| enumerate | `enumerate_orders.py` |
| zip | `usuals_zip.py` |
| map | `brew_map.py`, `grand_total_map.py` |
| filter | `orders_filter.py` |
| 模式匹配 | `match_coffee_sequence.py`, `match_coffee_dictionary.py` |
| 聚合函数 | `average_orders.py` |

---

**制作时间:** 2025-11-25
**基于:** DeadSimplePython Chapter 09 示例代码
**规范参考:** `Python学习笔记规范_for_Javaer.md`