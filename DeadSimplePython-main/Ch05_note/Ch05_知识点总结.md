# Chapter 05 - 类型系统、作用域与可变性

> **本章主题**: Python 的类型系统、变量作用域和可变/不可变类型
> **面向**: 有 Java 开发经验的学习者

---

## 目录
1. [类型检查](#1-类型检查)
2. [类型转换与类型强制转换](#2-类型转换与类型强制转换)
3. [可变类型与不可变类型](#3-可变类型与不可变类型)
4. [变量作用域](#4-变量作用域)
5. [类属性与实例属性](#5-类属性与实例属性)
6. [鸭子类型 (Duck Typing)](#6-鸭子类型-duck-typing)
7. [常见陷阱与最佳实践](#7-常见陷阱与最佳实践)

---

## 1. 类型检查

### 1.1 获取和检查类型

#### Python 实现
```python
answer = 42

# 方式 1: 使用 type() 函数
print(type(answer))  # <class 'int'>

# 方式 2: 使用 isinstance() (推荐)
if isinstance(answer, int):
    print("What's the question?")

# type() 精确匹配
if type(answer) is int:
    print("It's exactly an int")
```

#### Java 对比
```java
Object answer = 42;

// 方式 1: 使用 getClass()
System.out.println(answer.getClass());  // class java.lang.Integer

// 方式 2: 使用 instanceof (推荐)
if (answer instanceof Integer) {
    System.out.println("What's the question?");
}

// 基本类型无法直接检查类型
int primitiveAnswer = 42;
// primitiveAnswer.getClass();  // 编译错误,基本类型没有方法
```

#### 🔑 核心差异
| 特性 | Python | Java |
|------|--------|------|
| 类型检查函数 | `type()`, `isinstance()` | `getClass()`, `instanceof` |
| 推荐方式 | `isinstance(obj, Class)` | `obj instanceof Class` |
| 精确匹配 | `type(obj) is Class` | `obj.getClass() == Class.class` |
| 继承支持 | `isinstance()` 支持 | `instanceof` 支持 |
| 基本类型 | 所有类型都是对象 | 基本类型不是对象 |

#### 💡 Javaer 注意事项
- ✅ `isinstance(obj, Type)` ≈ Java 的 `obj instanceof Type`
- ⚠️ `type(obj) is Type` 不检查继承,类似 Java 的 `getClass() == Class.class`
- ⚠️ **推荐使用 `isinstance()`** 而不是 `type()`,因为支持继承
- 📌 记忆: "isinstance 更灵活,支持继承和多态"

**相关文件:** `types.py:1-7`

---

## 2. 类型转换与类型强制转换

### 2.1 显式类型转换 (Conversion)

#### Python 实现
```python
# 字符串转数字
life_universe_everything = "42"
answer = float(life_universe_everything)  # "42" -> 42.0

print(type(answer))  # <class 'float'>
print(answer)        # 42.0

# 常见转换函数
int("42")      # 字符串 -> 整数
float("3.14")  # 字符串 -> 浮点数
str(42)        # 数字 -> 字符串
bool(1)        # 其他类型 -> 布尔值
```

#### Java 对比
```java
// 字符串转数字
String lifeUniverseEverything = "42";
double answer = Double.parseDouble(lifeUniverseEverything);

System.out.println(answer.getClass());  // class java.lang.Double
System.out.println(answer);             // 42.0

// 常见转换方法
Integer.parseInt("42");        // 字符串 -> int
Double.parseDouble("3.14");    // 字符串 -> double
String.valueOf(42);            // 数字 -> 字符串
Boolean.parseBoolean("true");  // 字符串 -> boolean

// 包装类型转换
Integer num = 42;
double d = num.doubleValue();  // Integer -> double
```

#### 🔑 核心差异
| 特性 | Python | Java |
|------|--------|------|
| 转换语法 | `Type(value)` | `Type.parseXxx(value)` |
| 转换函数 | `int()`, `float()`, `str()` | `parseInt()`, `parseDouble()`, `valueOf()` |
| 异常处理 | `ValueError` | `NumberFormatException` |
| 自动装箱 | 不需要 (都是对象) | 自动装箱/拆箱 |

#### 💡 Javaer 注意事项
- ✅ Python 的 `int("42")` ≈ Java 的 `Integer.parseInt("42")`
- ⚠️ Python 转换失败抛出 `ValueError`,Java 抛出 `NumberFormatException`
- ✅ Python 的类型转换更简洁,统一使用 `Type(value)` 形式
- 📌 记忆: "Python 用构造函数语法转换类型"

**相关文件:** `conversion.py:1-6`

### 2.2 隐式类型强制转换 (Coercion)

#### Python 实现
```python
# 自动类型提升
print(42.5)      # 自动转换为字符串输出
x = 5 + 1.5      # int + float -> float (6.5)
y = 5 + True     # int + bool -> int (6), 但不推荐

# 字符串拼接不会自动转换
# result = "Number: " + 42  # TypeError!
result = "Number: " + str(42)  # 需要显式转换
```

#### Java 对比
```java
// 自动类型提升
System.out.println(42.5);      // 自动转换为字符串输出
double x = 5 + 1.5;            // int + double -> double (6.5)
int y = 5 + (true ? 1 : 0);    // boolean 不能直接参与运算

// 字符串拼接自动转换
String result = "Number: " + 42;  // 自动转换为 "Number: 42"
```

#### 🔑 核心差异
| 特性 | Python | Java |
|------|--------|------|
| 数字类型提升 | ✅ 自动 | ✅ 自动 |
| 字符串拼接 | ❌ 需显式转换 | ✅ 自动转换 |
| Boolean 运算 | ⚠️ 可以,但不推荐 | ❌ 不允许 |
| 规则 | 较少的隐式转换 | 较多的隐式转换 |

#### 💡 Javaer 注意事项
- ⚠️ **Python 字符串拼接不会自动转换类型**,Java 会自动转换
- ⚠️ Python 中 `True` 可以参与数学运算 (True=1, False=0),但**强烈不推荐**
- ✅ 数字类型提升规则类似: int + float -> float
- 📌 记忆: "Python 更显式,避免意外的类型转换"

**相关文件:** `coercion.py:1-3`

---

## 3. 可变类型与不可变类型

### 3.1 不可变类型 (Immutable Types)

#### Python 实现
```python
eggs = 12
carton = eggs         # carton 指向相同的对象
print(eggs is carton) # True (同一个对象)

eggs += 1             # 创建新对象!
print(eggs is carton) # False (不同对象)
print(eggs)           # 13
print(carton)         # 12 (原值未变)
```

**Python 不可变类型:**
- `int`, `float`, `bool`
- `str` (字符串)
- `tuple` (元组)
- `frozenset`
- `bytes`

#### Java 对比
```java
// 基本类型 (值类型)
int eggs = 12;
int carton = eggs;    // 复制值
eggs += 1;
System.out.println(eggs);    // 13
System.out.println(carton);  // 12

// String (不可变引用类型)
String str1 = "Hello";
String str2 = str1;
str1 += " World";     // 创建新对象
System.out.println(str1);  // "Hello World"
System.out.println(str2);  // "Hello"
```

#### 🔑 核心差异
| 特性 | Python 不可变类型 | Java 值类型/String |
|------|------------------|-------------------|
| 赋值行为 | 引用赋值 | 值复制 (基本类型) |
| 修改后 | 创建新对象 | 创建新对象 |
| 内存优化 | 小整数缓存池 | String 常量池 |
| 线程安全 | ✅ 天然安全 | ✅ 天然安全 |

#### 💡 Javaer 注意事项
- ⚠️ Python 的 `int` 是引用类型,但行为类似 Java 的基本类型
- ✅ Python 的 `str` = Java 的 `String`,都是不可变的
- ⚠️ Python 中 `+=` 对不可变类型会创建新对象
- 📌 记忆: "不可变类型修改时总是创建新对象"

**相关文件:** `immutable_types.py:1-7`

### 3.2 可变类型 (Mutable Types)

#### Python 实现
```python
temps = [87, 76, 79]
highs = temps           # highs 和 temps 指向同一个列表
print(temps is highs)   # True (同一个对象)

temps += [81]           # 修改原对象!
print(temps is highs)   # True (仍然是同一个对象)
print(highs)            # [87, 76, 79, 81]
print(temps)            # [87, 76, 79, 81]
```

**Python 可变类型:**
- `list` (列表)
- `dict` (字典)
- `set` (集合)
- `bytearray`
- 自定义类的对象

#### Java 对比
```java
// 引用类型 (默认可变)
List<Integer> temps = new ArrayList<>(Arrays.asList(87, 76, 79));
List<Integer> highs = temps;  // 两个变量指向同一个对象

System.out.println(temps == highs);  // true (同一个对象)

temps.add(81);          // 修改原对象
System.out.println(temps == highs);  // true (仍然是同一个对象)
System.out.println(highs);  // [87, 76, 79, 81]
System.out.println(temps);  // [87, 76, 79, 81]
```

#### 🔑 核心差异
| 特性 | Python 可变类型 | Java 引用类型 |
|------|----------------|--------------|
| 赋值行为 | 引用赋值 | 引用赋值 |
| 修改后 | 修改原对象 | 修改原对象 |
| 默认行为 | 大部分集合可变 | 大部分集合可变 |
| 不可变版本 | `tuple`, `frozenset` | `Collections.unmodifiableList()` |

#### 💡 Javaer 注意事项
- ✅ Python 的 `list` ≈ Java 的 `ArrayList`,都是可变的
- ✅ Python 的 `dict` ≈ Java 的 `HashMap`
- ⚠️ Python 的 `+=` 对可变类型是就地修改,对不可变类型是创建新对象
- 📌 记忆: "可变类型修改时影响所有引用"

**相关文件:** `mutable_types.py:1-7`

### 3.3 拷贝的陷阱

#### Python 实现
```python
import copy

class Taco:
    def __init__(self, toppings):
        # 错误: 直接赋值,共享引用
        # self.ingredients = toppings

        # 正确: 浅拷贝
        self.ingredients = copy.copy(toppings)

    def add_sauce(self, sauce):
        self.ingredients.append(sauce)

default_toppings = ["Lettuce", "Tomato", "Cheese", "Beef"]
mild_taco = Taco(default_toppings)

# 深拷贝对象
hot_taco = copy.deepcopy(mild_taco)
hot_taco.add_sauce("Salsa")

print(f"Hot: {hot_taco.ingredients}")   # ["Lettuce", ..., "Salsa"]
print(f"Mild: {mild_taco.ingredients}") # ["Lettuce", "Tomato", "Cheese", "Beef"]
print(f"Default: {default_toppings}")   # ["Lettuce", "Tomato", "Cheese", "Beef"]
```

**拷贝类型:**
- **浅拷贝** (`copy.copy()`): 拷贝对象,但内部引用仍共享
- **深拷贝** (`copy.deepcopy()`): 递归拷贝所有对象

#### Java 对比
```java
import java.util.*;

class Taco {
    private List<String> ingredients;

    public Taco(List<String> toppings) {
        // 错误: 直接赋值,共享引用
        // this.ingredients = toppings;

        // 正确: 浅拷贝
        this.ingredients = new ArrayList<>(toppings);
    }

    public void addSauce(String sauce) {
        this.ingredients.add(sauce);
    }

    // 深拷贝需要实现 Cloneable 或自定义方法
    public Taco deepCopy() {
        return new Taco(new ArrayList<>(this.ingredients));
    }
}

List<String> defaultToppings = new ArrayList<>(
    Arrays.asList("Lettuce", "Tomato", "Cheese", "Beef"));
Taco mildTaco = new Taco(defaultToppings);

// 深拷贝
Taco hotTaco = mildTaco.deepCopy();
hotTaco.addSauce("Salsa");

System.out.println("Hot: " + hotTaco.getIngredients());
System.out.println("Mild: " + mildTaco.getIngredients());
System.out.println("Default: " + defaultToppings);
```

#### 🔑 核心差异
| 特性 | Python | Java |
|------|--------|------|
| 浅拷贝 | `copy.copy()` | `new ArrayList<>(list)` |
| 深拷贝 | `copy.deepcopy()` | 需要实现 `Cloneable` 或自定义 |
| 便利性 | ✅ 内置模块 | ⚠️ 需要手动实现 |
| 默认行为 | 引用赋值 | 引用赋值 |

#### 💡 Javaer 注意事项
- ✅ Python 的 `copy.copy()` ≈ Java 的 `new ArrayList<>(original)`
- ✅ Python 的 `copy.deepcopy()` ≈ Java 的自定义深拷贝
- ⚠️ Python 的深拷贝更方便,Java 需要实现 `Cloneable` 接口
- 📌 记忆: "默认是引用传递,需要拷贝时用 copy 模块"

**相关文件:** `mutable_tacos.py:1-21`

### 3.4 元组内的可变对象

#### Python 实现
```python
# 元组是不可变的,但内部可以包含可变对象
scores_team_1 = [100, 95, 120]  # 可变列表
scores_team_2 = [45, 30, 10]
scores_team_3 = [200, 35, 190]

scores = (scores_team_1, scores_team_2, scores_team_3)  # 不可变元组

# 修改元组内的列表
scores_team_1[0] = 300
print(scores[0])  # [300, 95, 120] - 列表被修改了!

scores[0][0] = 400
print(scores[0])  # [400, 95, 120]

# 但不能替换元组的元素
# scores[0] = [1, 2, 3]  # TypeError!
```

#### Java 对比
```java
// 使用 final 修饰的引用
List<Integer> scoresTeam1 = new ArrayList<>(Arrays.asList(100, 95, 120));
List<Integer> scoresTeam2 = new ArrayList<>(Arrays.asList(45, 30, 10));
List<Integer> scoresTeam3 = new ArrayList<>(Arrays.asList(200, 35, 190));

// final 数组/列表
final List<List<Integer>> scores = Arrays.asList(scoresTeam1, scoresTeam2, scoresTeam3);

// 可以修改列表内容
scoresTeam1.set(0, 300);
System.out.println(scores.get(0));  // [300, 95, 120]

scores.get(0).set(0, 400);
System.out.println(scores.get(0));  // [400, 95, 120]

// 但不能替换列表引用
// scores.set(0, new ArrayList<>());  // UnsupportedOperationException
```

#### 🔑 核心差异
| 特性 | Python tuple | Java final 集合 |
|------|--------------|----------------|
| 不可变性 | 元组结构不可变 | 引用不可变 |
| 内部元素 | 可以是可变对象 | 可以是可变对象 |
| 修改内容 | ✅ 可以修改内部对象 | ✅ 可以修改内部对象 |
| 替换元素 | ❌ 不能替换 | ❌ 不能替换 |

#### 💡 Javaer 注意事项
- ✅ Python 的 `tuple` ≈ Java 的 `final List` (引用不可变)
- ⚠️ **不可变容器内可以包含可变对象**
- ⚠️ `tuple` 只保证结构不变,不保证内容不变
- 📌 记忆: "元组冻结结构,不冻结内容"

**相关文件:** `team_scores.py:1-11`

---

## 4. 变量作用域

### 4.1 局部作用域 (Local Scope)

#### Python 实现

```python
def spam():
    message = "Spam"      # 局部变量
    word = "spam"         # 局部变量

    for _ in range(100):
        separator = ", "  # for 循环内的变量也是函数局部变量!
        message = separator + word

    message += separator  # 仍然可以访问 separator
    message += "spam!"
    return message

# print(message)  # NameError: name 'message' is not defined
output = spam()
print(output)
```

#### Java 对比
```java
public String spam() {
    String message = "Spam";      // 方法局部变量
    String word = "spam";         // 方法局部变量

    String separator = null;
    for (int i = 0; i < 100; i++) {
        separator = ", ";         // for 内部,但在 Java 中仍是方法作用域
        message = separator + word;
    }

    message += separator;  // 可以访问 separator
    message += "spam!";
    return message;
}

// System.out.println(message);  // 编译错误: 找不到符号
String output = spam();
System.out.println(output);
```

#### 🔑 核心差异
| 特性 | Python | Java |
|------|--------|------|
| 函数作用域 | ✅ 函数级 | ✅ 方法级 |
| 块级作用域 | ❌ 无块级作用域 | ✅ 有块级作用域 (Java 7+) |
| for 循环变量 | 函数作用域 | 块级作用域 (Java 5+) |
| 变量提升 | ❌ 不存在 | ❌ 不存在 |

#### 💡 Javaer 注意事项
- ⚠️ **Python 没有块级作用域**,for/if 内的变量在整个函数中可见
- ✅ Python 函数作用域 ≈ Java 方法作用域
- ⚠️ Java 的 for 循环变量是块级作用域,Python 是函数级
- 📌 记忆: "Python 只有函数作用域,没有块级作用域"

**相关文件:** `local_scope.py:1-16`

### 4.2 全局作用域 (Global Scope)

#### Python 实现
```python
high_score = 10  # 全局变量

def score():
    global high_score       # 声明使用全局变量
    new_score = 465
    if new_score > high_score:
        print("New high score")
        high_score = new_score  # 修改全局变量

score()
print(high_score)  # 465
```

**注意事项:**
```python
current_score = 0

def score():
    global current_score
    new_score = 465
    current_score = new_score  # 必须用 global 声明,否则创建局部变量

score()
print(current_score)  # 465
```

#### Java 对比
```java
public class ScoreTracker {
    // 类级别的静态变量 (类似全局变量)
    private static int highScore = 10;

    public static void score() {
        int newScore = 465;  // 局部变量
        if (newScore > highScore) {
            System.out.println("New high score");
            highScore = newScore;  // 直接访问,无需声明
        }
    }

    public static void main(String[] args) {
        score();
        System.out.println(highScore);  // 465
    }
}
```

#### 🔑 核心差异
| 特性 | Python | Java |
|------|--------|------|
| 全局变量 | 模块级全局变量 | 静态类变量 |
| 修改需声明 | ✅ 需要 `global` 关键字 | ❌ 直接访问 |
| 只读访问 | ✅ 不需要声明 | ✅ 直接访问 |
| 作用范围 | 整个模块 | 整个类/应用 |

#### 💡 Javaer 注意事项
- ⚠️ **Python 修改全局变量必须用 `global` 声明**,否则创建同名局部变量
- ✅ Python 全局变量 ≈ Java 的 `static` 类变量
- ⚠️ 只读访问全局变量不需要 `global` 声明
- 📌 记忆: "改全局用 global,读全局不需要"

**相关文件:** `global_scope.py:1-13`, `global_scope_gotcha.py:1-11`

### 4.3 非局部作用域 (Nonlocal Scope)

#### Python 实现
```python
spam = True  # 全局变量

def order():
    eggs = 12  # 外层函数的局部变量

    def cook():
        nonlocal eggs  # 声明使用外层函数的变量

        if spam:       # 访问全局变量
            print("Spam!")

        if eggs:
            eggs -= 1  # 修改外层函数的变量
            print("...and eggs.")

    cook()
    print(f"Remaining eggs: {eggs}")  # 11

order()
```

#### Java 对比
Java 没有直接对应的概念,但可以使用类的实例变量模拟:

```java
public class Order {
    private boolean spam = true;  // 类变量

    public void order() {
        // Java 闭包要求变量是 final 或 effectively final
        final int[] eggs = {12};  // 使用数组绕过 final 限制

        Runnable cook = new Runnable() {
            @Override
            public void run() {
                if (spam) {
                    System.out.println("Spam!");
                }

                if (eggs[0] > 0) {
                    eggs[0]--;  // 修改数组内容
                    System.out.println("...and eggs.");
                }
            }
        };

        cook.run();
        System.out.println("Remaining eggs: " + eggs[0]);  // 11
    }
}
```

#### 🔑 核心差异
| 特性 | Python `nonlocal` | Java 闭包 |
|------|------------------|----------|
| 修改外层变量 | ✅ `nonlocal` 声明 | ⚠️ 需要 final (可用技巧绕过) |
| 语法支持 | ✅ 原生支持 | ⚠️ 受限制 |
| 嵌套函数 | ✅ 常用 | ⚠️ 较少用 |

#### 💡 Javaer 注意事项
- ⚠️ Python 的 `nonlocal` 用于访问外层函数的变量
- ✅ Java 的闭包变量必须是 final 或 effectively final
- ⚠️ Python 的嵌套函数比 Java 的内部类更常用和简洁
- 📌 记忆: "nonlocal 修改外层,global 修改全局"

**相关文件:** `nonlocal.py:1-20`

---

## 5. 类属性与实例属性

### 5.1 类属性 (Class Attributes)

#### Python 实现
```python
class Nutrimatic:
    output = "Something almost, but not quite, entirely unlike tea."  # 类属性

    def request(self, beverage):
        return self.output  # 通过实例访问类属性

machine = Nutrimatic()
mug = machine.request("Tea")
print(mug)  # 输出类属性值

# 三种访问方式
print(machine.output)     # 通过实例访问
print(Nutrimatic.output)  # 通过类访问 (推荐)
```

#### Java 对比
```java
public class Nutrimatic {
    // 静态变量 (类变量)
    public static String output = "Something almost, but not quite, entirely unlike tea.";

    public String request(String beverage) {
        return output;  // 访问类变量
    }

    public static void main(String[] args) {
        Nutrimatic machine = new Nutrimatic();
        String mug = machine.request("Tea");
        System.out.println(mug);

        // 两种访问方式
        System.out.println(machine.output);     // 通过实例 (不推荐)
        System.out.println(Nutrimatic.output);  // 通过类 (推荐)
    }
}
```

#### 🔑 核心差异
| 特性 | Python 类属性 | Java 静态变量 |
|------|--------------|--------------|
| 定义位置 | 类体内,方法外 | `static` 修饰符 |
| 访问方式 | 实例和类都可以 | 实例和类都可以 |
| 推荐访问 | `Class.attr` | `Class.attr` |
| 共享性 | 所有实例共享 | 所有实例共享 |

#### 💡 Javaer 注意事项
- ✅ Python 的类属性 = Java 的 `static` 变量
- ⚠️ Python 可以通过实例访问类属性,但推荐用类名访问
- ⚠️ 如果实例创建同名属性,会覆盖类属性 (只对该实例)
- 📌 记忆: "类属性是所有实例共享的"

**相关文件:** `class_attributes.py:1-13`

---

## 6. 鸭子类型 (Duck Typing)

### 6.1 鸭子类型哲学

> "如果它走起来像鸭子,叫起来像鸭子,那它就是鸭子"

#### Python 实现
```python
def calculate_age(birth_year, current_year):
    return (current_year - birth_year)

def calculate_third_age_year(current_age, current_year):
    return int(current_year - (current_age / 3))

# 不关心类型,只关心行为
birth_year = "1985"    # 字符串
birth_year = int(birth_year)  # 转换为 int

current_year = "2010"
current_year = int(current_year)

# 函数不检查类型,只要支持减法运算即可
current_age = calculate_age(birth_year, current_year)
third_age_year = calculate_third_age_year(current_age, current_year)
print(third_age_year)
```

#### Java 对比
```java
// Java 需要明确类型
public int calculateAge(int birthYear, int currentYear) {
    return currentYear - birthYear;
}

public int calculateThirdAgeYear(int currentAge, int currentYear) {
    return (int) (currentYear - (currentAge / 3.0));
}

// 使用
String birthYear = "1985";
int intBirthYear = Integer.parseInt(birthYear);

String currentYear = "2010";
int intCurrentYear = Integer.parseInt(currentYear);

int currentAge = calculateAge(intBirthYear, intCurrentYear);
int thirdAgeYear = calculateThirdAgeYear(currentAge, intCurrentYear);
System.out.println(thirdAgeYear);
```

#### 🔑 核心差异
| 特性 | Python 鸭子类型 | Java 静态类型 |
|------|----------------|--------------|
| 类型检查 | 运行时 | 编译时 |
| 类型声明 | 不需要 | 必须声明 |
| 灵活性 | 高 | 低 |
| 类型安全 | 低 | 高 |
| 多态方式 | 结构化类型 | 继承和接口 |

#### 💡 Javaer 注意事项
- ⚠️ Python 不检查参数类型,只要支持所需操作即可
- ✅ 鸭子类型提供更大灵活性,但容易在运行时出错
- ⚠️ Java 通过接口和继承实现多态,Python 通过鸭子类型
- 📌 记忆: "Python 看行为,Java 看类型"

**相关文件:** `duck_typing_feels_better.py:1-17`

---

## 7. 常见陷阱与最佳实践

### 7.1 避免使用 System Hungarian 命名

#### 错误示例 (不推荐)
```python
# evils_of_system_hungarian.py - 过度标注类型
def calculate_age(intBirthYear, intCurrentYear):
    intAge = intCurrentYear - intBirthYear
    return intAge

def calculate_third_age_year(intCurrentAge, intCurrentYear):
    floatThirdAge = intCurrentAge / 3
    floatCurrentYear = float(intCurrentYear)
    floatThirdAgeYear = floatCurrentYear - floatThirdAge
    intThirdAgeYear = int(floatThirdAgeYear)
    return intThirdAgeYear

strBirthYear = "1985"
intBirthYear = int(strBirthYear)
strCurrentYear = "2010"
intCurrentYear = int(strCurrentYear)
```

#### 正确示例 (推荐)
```python
# duck_typing_feels_better.py - 简洁命名
def calculate_age(birth_year, current_year):
    return (current_year - birth_year)

def calculate_third_age_year(current_age, current_year):
    return int(current_year - (current_age / 3))

birth_year = "1985"
birth_year = int(birth_year)
current_year = "2010"
current_year = int(current_year)
```

#### 💡 Javaer 注意事项
- ❌ 不要在变量名中标注类型 (`intAge`, `strName`)
- ✅ 使用描述性的变量名 (`age`, `name`)
- ⚠️ Java 需要类型声明,但变量名不应重复类型信息
- 📌 记忆: "让类型检查交给工具,变量名描述含义"

**相关文件:** `evils_of_system_hungarian.py`, `duck_typing_feels_better.py`

### 7.2 避免修改函数参数

#### 陷阱示例
```python
def find_lowest(temperatures):
    temperatures.sort()  # 修改了原列表!
    print(temperatures[0])

temps = [85, 76, 79, 72, 81]
find_lowest(temps)
print(temps)  # [72, 76, 79, 81, 85] - 原列表被修改了!
```

#### 正确做法
```python
def find_lowest(temperatures):
    sorted_temps = sorted(temperatures)  # 返回新列表
    print(sorted_temps[0])

temps = [85, 76, 79, 72, 81]
find_lowest(temps)
print(temps)  # [85, 76, 79, 72, 81] - 原列表未变
```

#### 💡 Javaer 注意事项
- ⚠️ Python 的可变对象传递的是引用,修改会影响原对象
- ✅ 使用返回新对象的方法 (`sorted()` vs `sort()`)
- ⚠️ Java 也有同样的问题,集合对象传递的是引用
- 📌 记忆: "不要修改参数,返回新对象"

**相关文件:** `lowest_temp.py:1-10`

### 7.3 列表乘法陷阱

#### 陷阱示例
```python
# 错误: 创建的是同一个列表的多个引用
board = [["-"] * 3] * 3
board[1][0] = "X"

# 所有行都被修改了!
for row in board:
    print(f"{row[0]} {row[1]} {row[2]}")
# 输出:
# X - -
# X - -
# X - -
```

#### 正确做法
```python
# 正确: 使用列表推导式创建独立的列表
board = [["-"] * 3 for _ in range(3)]
board[1][0] = "X"

for row in board:
    print(f"{row[0]} {row[1]} {row[2]}")
# 输出:
# - - -
# X - -
# - - -
```

#### 💡 Javaer 注意事项
- ⚠️ `[["-"] * 3] * 3` 创建的是同一个列表的 3 个引用
- ✅ 使用列表推导式创建独立对象
- ⚠️ Java 中用循环初始化二维数组不会有这个问题
- 📌 记忆: "列表乘法复制引用,不复制对象"

**相关文件:** `tic_tac_toe.py:1-8`

---

## 8. 总结对比表

### Python vs Java 类型系统

| 特性 | Python | Java |
|------|--------|------|
| **类型系统** | 动态类型 | 静态类型 |
| **类型检查** | 运行时 | 编译时 |
| **类型声明** | 可选 (类型提示) | 必需 |
| **类型转换** | `Type(value)` | `Type.parseXxx(value)` |
| **不可变类型** | int, str, tuple | 基本类型, String |
| **可变类型** | list, dict, set | 集合类 |
| **拷贝** | `copy` 模块 | 实现 Cloneable |
| **作用域** | 函数作用域 | 方法和块级作用域 |
| **全局变量** | `global` 关键字 | 静态变量 |
| **鸭子类型** | ✅ 核心特性 | ❌ 需要接口 |

---

## 9. 学习路径建议

1. **理解动态类型** - Python 不需要类型声明
2. **区分可变/不可变** - 理解赋值和修改的区别
3. **掌握作用域规则** - 函数、全局、非局部
4. **学习拷贝机制** - 浅拷贝 vs 深拷贝
5. **实践鸭子类型** - 面向协议编程
6. **避免常见陷阱** - 可变默认参数、列表乘法等

---

## 📊 参考文件索引

| 主题 | 文件名 |
|------|--------|
| 类型检查 | `types.py` |
| 类型转换 | `conversion.py`, `coercion.py` |
| 不可变类型 | `immutable_types.py` |
| 可变类型 | `mutable_types.py`, `team_scores.py` |
| 拷贝机制 | `mutable_tacos.py` |
| 作用域 | `local_scope.py`, `global_scope.py`, `nonlocal.py` |
| 类属性 | `class_attributes.py` |
| 鸭子类型 | `duck_typing_feels_better.py` |
| 常见陷阱 | `lowest_temp.py`, `tic_tac_toe.py`, `evils_of_system_hungarian.py` |

---

**制作时间:** 2025-11-25
**基于:** DeadSimplePython Chapter 05 示例代码
**规范参考:** `Python学习笔记规范_for_Javaer.md`