# Chapter 18 - 包管理与项目结构 (Package Management and Project Structure)

> **本章主题**: Python 的包管理和项目组织
> **面向**: 有 Java 开发经验的学习者

---

## 核心知识点

### 1. 包管理工具

#### Python
```bash
# pip - Python 包管理器
pip install requests
pip install -r requirements.txt
pip freeze > requirements.txt

# pipenv - 虚拟环境 + 依赖管理
pipenv install requests
pipenv shell

# poetry - 现代包管理
poetry add requests
poetry install
```

#### Java 对比
```bash
# Maven
mvn install
# 依赖在 pom.xml 中定义

# Gradle
gradle build
# 依赖在 build.gradle 中定义
```

#### 💡 Javaer 注意事项
- ✅ `pip` + `requirements.txt` ≈ Maven + `pom.xml`
- ✅ `poetry` ≈ Gradle (更现代)
- 📌 记忆: "pip = Python 的包管理器"

---

### 2. 虚拟环境

#### Python

```bash
# venv - 创建虚拟环境
python -m venv myenv
source myenv/bin/activate  # Linux/Mac
myenv\Scripts\activate     # Windows

# 退出虚拟环境
deactivate
```

#### Java 对比
Java 没有虚拟环境概念,但有类似的隔离机制:
- Maven/Gradle 的依赖管理
- Docker 容器隔离

#### 💡 Javaer 注意事项
- ⚠️ Python 虚拟环境隔离项目依赖
- ✅ 每个项目应该有独立的虚拟环境
- 📌 记忆: "虚拟环境 = 项目依赖隔离"

---

### 3. 项目结构

#### Python 项目

```
my_project/
├── pyproject.toml      # 项目配置
├── README.md
├── src/
│   └── my_package/
│       ├── __init__.py
│       ├── module1.py
│       └── module2.py
├── tests/
│   ├── __init__.py
│   └── test_module1.py
└── requirements.txt
```

#### Java 项目 (Maven)
```
my-project/
├── pom.xml
├── README.md
├── src/
│   ├── main/
│   │   └── java/
│   │       └── com/example/
│   │           └── MyClass.java
│   └── test/
│       └── java/
│           └── com/example/
│               └── MyClassTest.java
└── target/
```

#### 💡 Javaer 注意事项
- ✅ Python 项目结构更扁平
- ⚠️ `__init__.py` 标识包
- 📌 记忆: "Python 项目更简洁"

---

## 总结

| 特性 | Python | Java |
|------|--------|------|
| 包管理器 | pip, poetry | Maven, Gradle |
| 依赖文件 | requirements.txt, pyproject.toml | pom.xml, build.gradle |
| 虚拟环境 | venv, virtualenv | 无 (Docker) |
| 项目结构 | 扁平 | 深层嵌套 |

---

**制作时间:** 2025-11-25
