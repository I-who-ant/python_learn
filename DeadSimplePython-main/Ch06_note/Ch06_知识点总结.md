# Chapter 06 - 函数进阶

> **本章主题**: Python 函数的高级特性和技巧
> **面向**: 有 Java 开发经验的学习者

---

## 目录
1. [函数参数进阶](#1-函数参数进阶)
2. [递归函数](#2-递归函数)
3. [Lambda 表达式](#3-lambda-表达式)
4. [闭包 (Closures)](#4-闭包-closures)
5. [装饰器 (Decorators)](#5-装饰器-decorators)
6. [可变默认参数陷阱](#6-可变默认参数陷阱)
7. [函数式编程技巧](#7-函数式编程技巧)

---

## 1. 函数参数进阶

### 1.1 位置参数与关键字参数

#### Python 实现
```python
import random

def roll_dice(sides=6, dice=1):
    return tuple(random.randint(1, sides) for _ in range(dice))

# 多种调用方式
roll_dice(6, 5)           # 位置参数
roll_dice(sides=6, dice=5)  # 关键字参数
roll_dice(dice=5, sides=6)  # 关键字参数可以打乱顺序
roll_dice(dice=5)          # 混合: 默认 sides=6
roll_dice(6, dice=5)       # 混合: 位置 + 关键字
```

#### Java 对比
```java
// Java 没有关键字参数和默认参数
public class DiceRoller {
    // 方法重载模拟默认参数
    public List<Integer> rollDice() {
        return rollDice(6, 1);
    }

    public List<Integer> rollDice(int sides) {
        return rollDice(sides, 1);
    }

    public List<Integer> rollDice(int sides, int dice) {
        List<Integer> results = new ArrayList<>();
        Random random = new Random();
        for (int i = 0; i < dice; i++) {
            results.add(random.nextInt(sides) + 1);
        }
        return results;
    }

    // 使用 Builder 模式模拟关键字参数
    public static class DiceRollBuilder {
        private int sides = 6;
        private int dice = 1;

        public DiceRollBuilder sides(int sides) {
            this.sides = sides;
            return this;
        }

        public DiceRollBuilder dice(int dice) {
            this.dice = dice;
            return this;
        }

        public List<Integer> roll() {
            return new DiceRoller().rollDice(sides, dice);
        }
    }
}

// 使用
new DiceRoller().rollDice(6, 5);
new DiceRollBuilder().sides(6).dice(5).roll();
new DiceRollBuilder().dice(5).roll();  // sides 默认为 6
```

#### 🔑 核心差异
| 特性 | Python | Java |
|------|--------|------|
| 默认参数 | ✅ 原生支持 | ❌ 需要方法重载 |
| 关键字参数 | ✅ 原生支持 | ❌ 需要 Builder 模式 |
| 参数顺序 | 关键字参数可打乱 | 必须按顺序 |
| 灵活性 | 高 | 低 |

#### 💡 Javaer 注意事项
- ✅ Python 的默认参数 + 关键字参数 ≈ Java 的方法重载 + Builder 模式
- ⚠️ 关键字参数让代码更清晰,尤其是参数多时
- ⚠️ 混合使用时,位置参数必须在关键字参数之前
- 📌 记忆: "Python 参数超级灵活,Java 需要额外设计模式"

**相关文件:** `dice_roll.py:7-25`

### 1.2 仅限关键字参数 (Keyword-Only)

#### Python 实现
```python
import random

# * 后面的参数只能用关键字传递
def roll_dice(*, sides=6, dice=1):
    return tuple(random.randint(1, sides) for _ in range(dice))

# 正确的调用
dice_cup = roll_dice(sides=6, dice=5)  # ✅ OK

# 错误的调用
dice_cup = roll_dice(6, 5)  # ❌ TypeError: 必须使用关键字参数
```

#### Java 对比
Java 没有直接对应的特性,但可以通过设计模式模拟:

```java
public class DiceRoller {
    // 私有构造函数,强制使用 Builder
    private DiceRoller() {}

    public static class Builder {
        private int sides = 6;
        private int dice = 1;

        // 只能通过方法设置参数,类似"关键字参数"
        public Builder withSides(int sides) {
            this.sides = sides;
            return this;
        }

        public Builder withDice(int dice) {
            this.dice = dice;
            return this;
        }

        public List<Integer> roll() {
            // 实现掷骰子逻辑
            return new ArrayList<>();
        }
    }

    public static Builder builder() {
        return new Builder();
    }
}

// 使用 - 强制使用"关键字"方式
DiceRoller.builder()
    .withSides(6)
    .withDice(5)
    .roll();

// 不能直接传位置参数
// DiceRoller.roll(6, 5);  // 不存在这个方法
```

#### 🔑 核心差异
| 特性 | Python Keyword-Only | Java Builder |
|------|---------------------|--------------|
| 语法支持 | ✅ `*` 分隔符 | ⚠️ 需要 Builder 模式 |
| 编译器检查 | ✅ 自动 | ⚠️ 需要设计 |
| 可读性 | 高 | 中等 |
| 实现成本 | 低 | 高 |

#### 💡 Javaer 注意事项
- ⚠️ Python 的 `*` 强制后面参数必须用关键字传递
- ✅ 提高代码可读性,避免参数顺序错误
- ⚠️ Java 需要 Builder 模式才能达到类似效果
- 📌 记忆: "* 是关键字参数的守门员"

**相关文件:** `dice_roll_keyword_only.py:5-12`

### 1.3 仅限位置参数 (Positional-Only, Python 3.8+)

#### Python 实现
```python
import random

# / 前面的参数只能用位置传递
def roll_dice(dice=1, /, sides=6):
    return tuple(random.randint(1, sides) for _ in range(dice))

# 正确的调用
roll_dice(4, 20)         # ✅ OK: dice=4, sides=20
roll_dice(4)             # ✅ OK: dice=4, sides=6
roll_dice(sides=20)      # ✅ OK: dice=1, sides=20
roll_dice(4, sides=20)   # ✅ OK: dice=4, sides=20

# 错误的调用
roll_dice(dice=4)            # ❌ TypeError
roll_dice(dice=4, sides=20)  # ❌ TypeError
```

#### Java 对比
Java 的所有参数本质上都是位置参数:

```java
public class DiceRoller {
    // Java 方法参数都是位置参数
    public List<Integer> rollDice(int dice, int sides) {
        // 实现
        return new ArrayList<>();
    }

    // 方法重载提供默认值
    public List<Integer> rollDice(int dice) {
        return rollDice(dice, 6);
    }

    public List<Integer> rollDice() {
        return rollDice(1, 6);
    }
}

// 使用 - 只能按位置传递
new DiceRoller().rollDice(4, 20);
new DiceRoller().rollDice(4);

// 不能使用"关键字"方式
// new DiceRoller().rollDice(dice: 4);  // 编译错误
```

#### 🔑 核心差异
| 特性 | Python Positional-Only | Java 标准参数 |
|------|----------------------|-------------|
| 语法标记 | ✅ `/` 分隔符 | ❌ 默认行为 |
| 混合模式 | ✅ 可混合位置和关键字 | ❌ 只有位置 |
| 灵活性 | 高 | 低 |

#### 💡 Javaer 注意事项
- ✅ Python 的 `/` 前参数类似 Java 的标准参数(只能位置传递)
- ⚠️ `/` 后的参数可以用关键字或位置传递
- ⚠️ 可以同时使用 `/` 和 `*`: `def func(pos_only, /, normal, *, kw_only)`
- 📌 记忆: "/ 是位置参数的守门员"

**相关文件:** `dice_roll_positional_only.py:6-16`

### 1.4 可变参数 (*args 和 **kwargs)

#### Python 实现
```python
import random

# *args 接收任意数量的位置参数
def roll_dice(*dice):
    return tuple(random.randint(1, d) for d in dice)

dice_cup = roll_dice(6, 6, 6, 6, 6)  # 5个6面骰子
bunch_o_dice = roll_dice(20, 6, 8, 4)  # 不同面数的骰子

# **kwargs 接收任意数量的关键字参数
def call_something_else(func, *args, **kwargs):
    return func(*args, **kwargs)

def say_hi(name):
    print(f"Hello, {name}!")

call_something_else(say_hi, name="Bob")  # 转发参数
```

#### Java 对比
```java
public class DiceRoller {
    // Java 的 varargs (可变参数)
    public List<Integer> rollDice(int... dice) {
        List<Integer> results = new ArrayList<>();
        Random random = new Random();
        for (int sides : dice) {
            results.add(random.nextInt(sides) + 1);
        }1
        return results;
    }

    // Java 没有 **kwargs 等价物
    // 可以用 Map 模拟
    public void callSomethingElse(
        Consumer<Map<String, Object>> func,
        Map<String, Object> kwargs
    ) {
        func.accept(kwargs);
    }

    // 或使用函数式接口
    public <T> T callFunction(
        Function<String, T> func,
        String arg
    ) {
        return func.apply(arg);
    }
}

// 使用
new DiceRoller().rollDice(6, 6, 6, 6, 6);
new DiceRoller().rollDice(20, 6, 8, 4);

// 转发参数示例
new DiceRoller().callFunction(
    name -> "Hello, " + name + "!",
    "Bob"
);
```

#### 🔑 核心差异
| 特性 | Python | Java |
|------|--------|------|
| 可变位置参数 | `*args` | `Type... args` |
| 可变关键字参数 | `**kwargs` | ❌ 无 (用 Map) |
| 参数解包 | `func(*list, **dict)` | ⚠️ 手动处理 |
| 类型 | `args` 是 tuple, `kwargs` 是 dict | 数组 |

#### 💡 Javaer 注意事项
- ✅ Python 的 `*args` ≈ Java 的 `Type... args`
- ⚠️ Python 的 `**kwargs` 在 Java 中需要用 `Map<String, Object>` 模拟
- ⚠️ `*args` 和 `**kwargs` 常用于装饰器和包装函数
- 📌 记忆: "*args 收集位置,**kwargs 收集关键字"

**相关文件:** `dice_roll_variadic.py:4-16`, `variadic_relay.py:1-9`

---

## 2. 递归函数

### 2.1 基本递归

#### Python 实现

```python
import random

def roll_dice(sides, dice):
    # 基本情况: 没有骰子了
    if dice < 1:
        return ()

    # 递归情况
    roll = random.randint(1, sides)
    return (roll,) + roll_dice(sides, dice - 1)

dice_cup = roll_dice(6, 5)
print(dice_cup)
```



#### Java 对比
```java
public class DiceRoller {
    private Random random = new Random();

    public List<Integer> rollDice(int sides, int dice) {
        // 基本情况
        if (dice < 1) {
            return new ArrayList<>();
        }

        // 递归情况
        List<Integer> results = new ArrayList<>();
        results.add(random.nextInt(sides) + 1);
        results.addAll(rollDice(sides, dice - 1));
        return results;
    }
}

// 使用
List<Integer> diceCup = new DiceRoller().rollDice(6, 5);
System.out.println(diceCup);
```

#### 🔑 核心差异
| 特性 | Python | Java |
|------|--------|------|
| 递归语法 | 相同 | 相同 |
| 尾递归优化 | ❌ 不支持 | ❌ 不支持 |
| 栈深度限制 | 约 1000 层 | 可配置 (-Xss) |
| 函数式风格 | ✅ 常用 | ⚠️ 较少用 |

#### 💡 Javaer 注意事项
- ✅ Python 和 Java 的递归语法基本相同
- ⚠️ **Python 默认递归深度限制约 1000 层** (可用 `sys.setrecursionlimit()` 修改)
- ⚠️ 两者都不支持尾递归优化,深递归容易栈溢出
- 📌 记忆: "递归要有基本情况和递归情况"

**相关文件:** `dice_roll_recursive.py:4-12`

### 2.2 递归与可变参数

#### Python 实现

```python
import random

def roll_dice(*dice):
    # 基本情况: 没有骰子了
    if not dice:
        return ()

    # 递归情况: 处理第一个骰子,递归处理剩余骰子
    roll = random.randint(1, dice[0])
    return (roll,) + roll_dice(*dice[1:])  # 解包剩余参数

dice_cup = roll_dice(6, 6, 6, 6, 6)
bunch_o_dice = roll_dice(20, 6, 8, 4)
```

#### Java 对比
```java
public class DiceRoller {
    private Random random = new Random();

    public List<Integer> rollDice(int... dice) {
        return rollDiceHelper(dice, 0);
    }

    private List<Integer> rollDiceHelper(int[] dice, int index) {
        // 基本情况
        if (index >= dice.length) {
            return new ArrayList<>();
        }

        // 递归情况
        List<Integer> results = new ArrayList<>();
        results.add(random.nextInt(dice[index]) + 1);
        results.addAll(rollDiceHelper(dice, index + 1));
        return results;
    }
}

// 使用
new DiceRoller().rollDice(6, 6, 6, 6, 6);
new DiceRoller().rollDice(20, 6, 8, 4);
```

#### 💡 Javaer 注意事项
- ⚠️ Python 的 `*dice[1:]` 解包剩余参数很简洁
- ⚠️ Java 需要额外的索引参数或数组切片
- ✅ Python 的切片 `dice[1:]` 创建新元组,不修改原数据
- 📌 记忆: "*解包参数,递归处理剩余"

**相关文件:** `dice_roll_variadic.py:4-16`

---

## 3. Lambda 表达式

### 3.1 基本 Lambda

#### Python 实现
```python
# Lambda 表达式: 匿名函数
add = lambda x, y: x + y
answer = add(20, 22)
print(answer)  # 42

# 直接使用 (不推荐)
result = (lambda x, y: x + y)(20, 22)
```

#### Java 对比
```java
// Java 8+ Lambda 表达式
BiFunction<Integer, Integer, Integer> add = (x, y) -> x + y;
int answer = add.apply(20, 22);
System.out.println(answer);  // 42

// 直接使用
int result = ((BiFunction<Integer, Integer, Integer>)
    (x, y) -> x + y).apply(20, 22);

// 函数式接口
@FunctionalInterface
interface Calculator {
    int calculate(int x, int y);
}

Calculator add = (x, y) -> x + y;
```

#### 🔑 核心差异
| 特性 | Python Lambda | Java Lambda |
|------|--------------|-------------|
| 语法 | `lambda args: expr` | `(args) -> expr` |
| 多语句 | ❌ 只能单表达式 | ✅ 可以多语句 |
| 类型声明 | 不需要 | 需要函数式接口 |
| 返回值 | 自动返回 | 自动返回 (或用 {}) |

#### 💡 Javaer 注意事项
- ✅ Python 的 `lambda` ≈ Java 的 Lambda 表达式
- ⚠️ **Python lambda 只能是单个表达式**,不能有语句块
- ⚠️ Java lambda 可以有多语句 (用花括号)
- ⚠️ Python 不推荐给 lambda 赋值,应该用 `def`
- 📌 记忆: "lambda 适合短小的一次性函数"

**相关文件:** `addition_lambda.py:1-3`

### 3.2 Lambda 作为参数

#### Python 实现
```python
# 排序时使用 lambda
people = [
    ("Jason", "McDonald"),
    ("Denis", "Pobedrya"),
    ("Daniel", "Foerster"),
]

# 按姓氏排序
by_last_name = sorted(people, key=lambda x: x[1])
print(by_last_name)

# 文本冒险游戏示例
def attempt(action, min_roll, outcome):
    roll = random.randint(1, 20)
    success = roll >= min_roll
    return outcome(success)  # 调用 lambda

# 使用 lambda 定义不同结果
attempt("Eating bread", 5,
        lambda success: (1, 0) if success else (-1, 0))

attempt("Fighting ice weasel", 15,
        lambda success: (0, 10) if success else (-10, 10))
```

#### Java 对比
```java
// 排序时使用 Lambda
List<Pair<String, String>> people = Arrays.asList(
    new Pair<>("Jason", "McDonald"),
    new Pair<>("Denis", "Pobedrya"),
    new Pair<>("Daniel", "Foerster")
);

// 按姓氏排序
List<Pair<String, String>> byLastName = people.stream()
    .sorted(Comparator.comparing(p -> p.getValue()))
    .collect(Collectors.toList());

// 或使用方法引用
people.sort(Comparator.comparing(Pair::getValue));

// 函数作为参数
public Pair<Integer, Integer> attempt(
    String action,
    int minRoll,
    Function<Boolean, Pair<Integer, Integer>> outcome
) {
    int roll = new Random().nextInt(20) + 1;
    boolean success = roll >= minRoll;
    return outcome.apply(success);
}

// 使用
attempt("Eating bread", 5,
    success -> success ? new Pair<>(1, 0) : new Pair<>(-1, 0));

attempt("Fighting ice weasel", 15,
    success -> success ? new Pair<>(0, 10) : new Pair<>(-10, 10));
```

#### 🔑 核心差异
| 特性 | Python | Java |
|------|--------|------|
| 作为参数 | 直接传递 | 需要函数式接口 |
| 排序 key | `key=lambda x: x[1]` | `Comparator.comparing(...)` |
| 条件表达式 | `x if cond else y` | `cond ? x : y` |

#### 💡 Javaer 注意事项
- ✅ Python 函数是第一类对象,可以直接传递
- ✅ Java 8+ 也支持 Lambda,但需要函数式接口
- ⚠️ Python 的 `key` 参数在排序中很常用
- 📌 记忆: "lambda 让回调函数更简洁"

**相关文件:** `sort_names.py:9`, `text_adventure_v1.py:40-43`

---

## 4. 闭包 (Closures)

### 4.1 基本闭包

#### Python 实现
```python
import random

def make_dice_cup(sides=6, dice=1):
    # 内部函数可以访问外部函数的变量
    def roll():
        return tuple(random.randint(1, sides) for _ in range(dice))

    return roll  # 返回内部函数

# 创建专用的掷骰子函数
roll_for_damage = make_dice_cup(sides=8, dice=5)
damage = roll_for_damage()  # 记住了 sides=8, dice=5
print(damage)
```

**工作原理:**
- 内部函数 `roll` 捕获了外部函数的 `sides` 和 `dice` 变量
- 即使 `make_dice_cup` 已经返回,这些变量仍然存在
- 每次调用 `make_dice_cup` 创建新的闭包实例

#### Java 对比
```java
public class DiceCupFactory {
    // Java 闭包: Lambda 捕获外部变量
    public Supplier<List<Integer>> makeDiceCup(int sides, int dice) {
        Random random = new Random();

        // Lambda 表达式创建闭包
        return () -> {
            List<Integer> results = new ArrayList<>();
            for (int i = 0; i < dice; i++) {
                results.add(random.nextInt(sides) + 1);
            }
            return results;
        };
    }

    // 或使用内部类 (传统方式)
    public Supplier<List<Integer>> makeDiceCupInnerClass(
        final int sides,
        final int dice
    ) {
        return new Supplier<List<Integer>>() {
            private Random random = new Random();

            @Override
            public List<Integer> get() {
                List<Integer> results = new ArrayList<>();
                for (int i = 0; i < dice; i++) {
                    results.add(random.nextInt(sides) + 1);
                }
                return results;
            }
        };
    }
}

// 使用
DiceCupFactory factory = new DiceCupFactory();
Supplier<List<Integer>> rollForDamage = factory.makeDiceCup(8, 5);
List<Integer> damage = rollForDamage.get();
System.out.println(damage);
```

#### 🔑 核心差异
| 特性 | Python 闭包 | Java 闭包 |
|------|------------|-----------|
| 语法 | 嵌套函数 | Lambda 或内部类 |
| 捕获变量 | 自动捕获 | effectively final |
| 修改变量 | ✅ 用 `nonlocal` | ❌ 需要技巧 (数组/对象) |
| 常用性 | ✅ 非常常用 | ⚠️ 较少用 |

#### 💡 Javaer 注意事项
- ✅ Python 闭包 ≈ Java 的 Lambda + 捕获变量
- ⚠️ Java 的闭包变量必须是 final 或 effectively final
- ⚠️ Python 闭包可以用 `nonlocal` 修改外部变量
- 📌 记忆: "闭包 = 函数 + 环境"

**相关文件:** `dice_cup_closure.py:4-13`

### 4.2 闭包与状态管理

#### Python 实现
```python
import random

def start_turn(limit, dice=5, sides=6):
    # limit 是闭包捕获的状态
    def roll():
        nonlocal limit  # 修改外部变量
        if limit < 1:
            return None
        limit -= 1
        return tuple(random.randint(1, sides) for _ in range(dice))

    return roll

# 每个回合有独立的状态
turn1 = start_turn(limit=3)
while toss := turn1():
    print(toss)  # 最多掷3次

turn2 = start_turn(limit=3)
while toss := turn2():
    print(toss)  # 又是新的3次
```

#### Java 对比
```java
public class TurnManager {
    // 使用类封装状态
    public static class Turn {
        private int limit;
        private final int dice;
        private final int sides;
        private final Random random = new Random();

        public Turn(int limit, int dice, int sides) {
            this.limit = limit;
            this.dice = dice;
            this.sides = sides;
        }

        public List<Integer> roll() {
            if (limit < 1) {
                return null;
            }
            limit--;
            List<Integer> results = new ArrayList<>();
            for (int i = 0; i < dice; i++) {
                results.add(random.nextInt(sides) + 1);
            }
            return results;
        }
    }

    public static Turn startTurn(int limit, int dice, int sides) {
        return new Turn(limit, dice, sides);
    }
}

// 使用
Turn turn1 = TurnManager.startTurn(3, 5, 6);
List<Integer> toss;
while ((toss = turn1.roll()) != null) {
    System.out.println(toss);
}

Turn turn2 = TurnManager.startTurn(3, 5, 6);
while ((toss = turn2.roll()) != null) {
    System.out.println(toss);
}
```

#### 💡 Javaer 注意事项
- ⚠️ Python 用闭包管理状态,Java 通常用类
- ⚠️ `nonlocal` 允许修改闭包捕获的变量
- ✅ 闭包提供了轻量级的状态封装
- 📌 记忆: "闭包可以替代简单的类"

**相关文件:** `dice_roll_turns.py:5-22`

---

## 5. 装饰器 (Decorators)

### 5.1 基本装饰器

#### Python 实现
```python
import functools

def character_action(func):
    @functools.wraps(func)  # 保留原函数元数据
    def wrapper(*args, **kwargs):
        # 前置操作
        if health <= 0:
            print(f"{character} is too weak.")
            return

        # 调用原函数
        result = func(*args, **kwargs)

        # 后置操作
        print(f"    Health: {health} | XP: {xp}")
        return result

    return wrapper

# 使用装饰器
@character_action
def eat_food(food):
    global health
    print(f"{character} ate {food}")
    health += 1

@character_action
def fight_monster(monster, strength):
    global health, xp
    if random.randint(1, 20) >= strength:
        print(f"{character} defeated {monster}.")
        xp += 10
    else:
        print(f"{character} flees from {monster}.")
        health -= 10
        xp += 5

# 调用被装饰的函数
eat_food("bread")
fight_monster("Imp", 15)
```

**装饰器等价于:**
```python
def eat_food(food):
    # ...

eat_food = character_action(eat_food)  # 手动装饰
```

#### Java 对比
Java 没有装饰器语法,但可以用设计模式实现:

```java
// 1. 使用装饰器模式
@FunctionalInterface
interface Action {
    void execute();
}

public class CharacterActionDecorator {
    private int health;
    private int xp;
    private String character;

    public Action wrap(Action action) {
        return () -> {
            // 前置检查
            if (health <= 0) {
                System.out.println(character + " is too weak.");
                return;
            }

            // 执行原操作
            action.execute();

            // 后置操作
            System.out.println("    Health: " + health + " | XP: " + xp);
        };
    }
}

// 使用
CharacterActionDecorator decorator = new CharacterActionDecorator();

Action eatFood = decorator.wrap(() -> {
    System.out.println(character + " ate bread");
    health++;
});

eatFood.execute();

// 2. 使用 AOP (Spring)
@Aspect
public class CharacterActionAspect {
    @Around("@annotation(CharacterAction)")
    public Object checkHealth(ProceedingJoinPoint pjp) throws Throwable {
        if (health <= 0) {
            System.out.println(character + " is too weak.");
            return null;
        }
        Object result = pjp.proceed();
        System.out.println("    Health: " + health + " | XP: " + xp);
        return result;
    }
}

@CharacterAction
public void eatFood(String food) {
    System.out.println(character + " ate " + food);
    health++;
}
```

#### 🔑 核心差异
| 特性 | Python 装饰器 | Java 等价方案 |
|------|--------------|--------------|
| 语法支持 | ✅ `@decorator` | ❌ 无 (用 AOP) |
| 实现方式 | 高阶函数 | 设计模式/AOP |
| 便利性 | 高 | 低 |
| 标准库 | `functools.wraps` | Spring AOP |

#### 💡 Javaer 注意事项
- ✅ Python 装饰器 ≈ Java 的装饰器模式 + AOP
- ⚠️ `@decorator` 只是语法糖,本质是函数包装
- ⚠️ `functools.wraps` 保留原函数的元数据 (名称、文档等)
- ⚠️ Java 通常用 Spring AOP 实现类似功能
- 📌 记忆: "装饰器 = 在不修改原函数的情况下增强功能"

**相关文件:** `text_adventure_v2.py:9-45`

### 5.2 装饰器的优势

**对比示例:**

#### 没有装饰器 (v1)
```python
def attempt(action, min_roll, outcome):
    global health, xp
    roll = random.randint(1, 20)
    if roll >= min_roll:
        result = True
    else:
        result = False

    scores = outcome(result)
    health = health + scores[0]
    xp = xp + scores[1]
    return result

# 重复的健康检查和结果显示逻辑
attempt("Eating bread", 5, lambda s: (1, 0) if s else (-1, 0))
attempt("Fighting ice weasel", 15, lambda s: (0, 10) if s else (-10, 10))
```

#### 使用装饰器 (v2)
```python
@character_action
def eat_food(food):
    global health
    print(f"{character} ate {food}")
    health += 1

@character_action
def fight_monster(monster, strength):
    # 健康检查和结果显示由装饰器自动处理
    ...

# 更清晰的调用
eat_food("bread")
fight_monster("Imp", 15)
```

#### 💡 Javaer 注意事项
- ✅ 装饰器实现了 DRY 原则 (Don't Repeat Yourself)
- ✅ 分离关注点: 业务逻辑 vs 横切关注点 (日志、验证等)
- ⚠️ 类似 Java 的 AOP,但语法更简洁
- 📌 记忆: "装饰器 = 代码复用 + 清晰分离"

---

## 6. 可变默认参数陷阱

### 6.1 陷阱示例

#### Python 错误示例
```python
# ❌ 危险: 可变默认参数
def fibonacci_next(series=[1, 1]):
    series.append(series[-1] + series[-2])
    return series

fib1 = fibonacci_next()
print(fib1)  # [1, 1, 2]

fib1 = fibonacci_next(fib1)
print(fib1)  # [1, 1, 2, 3]

fib2 = fibonacci_next()  # 期望 [1, 1, 2]
print(fib2)  # ❌ 实际输出: [1, 1, 2, 3, 5] - 被污染了!
```

**问题原因:**
- 默认参数在函数**定义时**创建,不是调用时
- 所有调用共享同一个列表对象
- 修改列表会影响后续调用

#### Python 正确做法
```python
# ✅ 正确: 使用 None 作为默认值
def fibonacci_next(series=None):
    if series is None:
        series = [1, 1]  # 每次调用创建新列表
    series.append(series[-1] + series[-2])
    return series

fib1 = fibonacci_next()
print(fib1)  # [1, 1, 2]

fib2 = fibonacci_next()
print(fib2)  # ✅ [1, 1, 2] - 正确!
```

#### Java 对比
Java 没有默认参数,但有类似陷阱:

```java
public class FibonacciGenerator {
    // ❌ 危险: 共享可变状态
    private static List<Integer> defaultSeries =
        new ArrayList<>(Arrays.asList(1, 1));

    public List<Integer> fibonacciNext() {
        return fibonacciNext(defaultSeries);  // 共享引用!
    }

    public List<Integer> fibonacciNext(List<Integer> series) {
        int next = series.get(series.size() - 1) +
                   series.get(series.size() - 2);
        series.add(next);
        return series;
    }

    // ✅ 正确: 每次创建新列表
    public List<Integer> fibonacciNextSafe() {
        return fibonacciNextSafe(null);
    }

    public List<Integer> fibonacciNextSafe(List<Integer> series) {
        if (series == null) {
            series = new ArrayList<>(Arrays.asList(1, 1));
        }
        int next = series.get(series.size() - 1) +
                   series.get(series.size() - 2);
        series.add(next);
        return series;
    }
}
```

#### 🔑 核心差异
| 特性 | Python | Java |
|------|--------|------|
| 默认参数 | 定义时创建 | 无默认参数 |
| 陷阱来源 | 可变默认参数 | 共享静态变量 |
| 解决方案 | `None` + 条件创建 | 方法重载 + 新建对象 |

#### 💡 Javaer 注意事项
- ⚠️ **永远不要使用可变对象作为默认参数** (list, dict, set)
- ✅ 使用 `None` 作为默认值,然后在函数内创建新对象
- ⚠️ 不可变对象 (int, str, tuple) 作为默认参数是安全的
- 📌 记忆: "可变默认参数 = 共享状态 = 潜在bug"

**相关文件:** `fibonacci.py:1-15`

---

## 7. 函数式编程技巧

### 7.1 函数作为第一类对象

**Python 特性:**
```python
# 函数可以赋值给变量
my_func = print
my_func("Hello")  # 等同于 print("Hello")

# 函数可以存储在数据结构中
operations = {
    'add': lambda x, y: x + y,
    'subtract': lambda x, y: x - y,
    'multiply': lambda x, y: x * y,
}

result = operations['add'](5, 3)  # 8

# 函数可以作为返回值 (闭包)
def make_multiplier(n):
    return lambda x: x * n

double = make_multiplier(2)
triple = make_multiplier(3)

print(double(5))  # 10
print(triple(5))  # 15
```

**Java 对比:**
```java
// Java 8+ 也支持函数作为第一类对象
BiFunction<Integer, Integer, Integer> add = (x, y) -> x + y;

// 存储在 Map 中
Map<String, BiFunction<Integer, Integer, Integer>> operations = Map.of(
    "add", (x, y) -> x + y,
    "subtract", (x, y) -> x - y,
    "multiply", (x, y) -> x * y
);

int result = operations.get("add").apply(5, 3);

// 返回函数
public Function<Integer, Integer> makeMultiplier(int n) {
    return x -> x * n;
}

Function<Integer, Integer> double = makeMultiplier(2);
Function<Integer, Integer> triple = makeMultiplier(3);
```

---

## 8. 总结对比表

### Python vs Java 函数特性

| 特性 | Python | Java |
|------|--------|------|
| **默认参数** | ✅ 原生支持 | ❌ 需要重载 |
| **关键字参数** | ✅ 原生支持 | ❌ 需要 Builder |
| **可变参数** | `*args`, `**kwargs` | `Type... args` |
| **Lambda** | 单表达式 | 单/多语句 |
| **闭包** | ✅ 常用 | ⚠️ 较少用 |
| **装饰器** | ✅ `@decorator` | ❌ 用 AOP |
| **递归** | 支持 (限制 ~1000) | 支持 (可配置) |
| **第一类对象** | ✅ 完全支持 | ✅ Java 8+ 支持 |

---

## 9. 学习路径建议

1. **掌握参数类型** - 位置、关键字、默认、可变
2. **理解递归** - 基本情况 + 递归情况
3. **学习 Lambda** - 简短函数的优雅写法
4. **实践闭包** - 状态封装和工厂模式
5. **使用装饰器** - 代码复用和 AOP
6. **避免陷阱** - 可变默认参数
7. **函数式思维** - 函数作为数据

---

## 📊 参考文件索引

| 主题 | 文件名 |
|------|--------|
| 函数参数 | `dice_roll.py`, `dice_roll_keyword_only.py`, `dice_roll_positional_only.py` |
| 可变参数 | `dice_roll_variadic.py`, `variadic_relay.py` |
| 递归 | `dice_roll_recursive.py` |
| Lambda | `addition_lambda.py`, `sort_names.py` |
| 闭包 | `dice_cup_closure.py`, `dice_cup_closure_recursive.py`, `dice_roll_turns.py` |
| 装饰器 | `text_adventure_v2.py` |
| 陷阱 | `fibonacci.py` |
| 综合应用 | `text_adventure_v1.py` |

---

**制作时间:** 2025-11-25
**基于:** DeadSimplePython Chapter 06 示例代码
**规范参考:** `Python学习笔记规范_for_Javaer.md`