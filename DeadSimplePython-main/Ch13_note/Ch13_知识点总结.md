# Chapter 13 - 多态与 Mixins (Polymorphism and Mixins)

> **本章主题**: Python 的多态、多重继承和 Mixin 模式
> **面向**: 有 Java 开发经验的学习者

---

## 核心知识点

### 1. Python 多态 (鸭子类型)

#### Python 实现
```python
# Python 不需要共同接口,只需要相同的方法
class Cat:
    def speak(self):
        return "Meow"

class Dog:
    def speak(self):
        return "Woof"

def make_speak(animal):
    print(animal.speak())  # 鸭子类型,不检查类型

make_speak(Cat())  # Meow
make_speak(Dog())  # Woof
```

#### Java 对比
```java
// Java 需要显式接口
interface Animal {
    String speak();
}

class Cat implements Animal {
    public String speak() {
        return "Meow";
    }
}

class Dog implements Animal {
    public String speak() {
        return "Woof";
    }
}

void makeSpeak(Animal animal) {
    System.out.println(animal.speak());
}
```

#### 💡 Javaer 注意事项
- ⚠️ Python 多态基于"鸭子类型",不需要显式接口
- ✅ Java 需要 interface 或继承实现多态
- 📌 记忆: "Python = 结构化类型,Java = 名义类型"

---

### 2. Mixins - 多重继承的最佳实践

#### Python 实现
```python
# Mixin: 提供额外功能的小类
class LogMixin:
    def log(self, message):
        print(f"[LOG] {message}")

class TimestampMixin:
    def timestamp(self):
        from datetime import datetime
        return datetime.now()

class Task(LogMixin, TimestampMixin):
    def __init__(self, name):
        self.name = name

    def execute(self):
        self.log(f"Executing {self.name}")
        print(f"At {self.timestamp()}")

task = Task("Deploy")
task.execute()
```

#### Java 对比
```java
// Java 使用接口 + 默认方法模拟 Mixin
interface LogMixin {
    default void log(String message) {
        System.out.println("[LOG] " + message);
    }
}

interface TimestampMixin {
    default LocalDateTime timestamp() {
        return LocalDateTime.now();
    }
}

class Task implements LogMixin, TimestampMixin {
    private String name;

    public void execute() {
        log("Executing " + name);
        System.out.println("At " + timestamp());
    }
}
```

#### 💡 Javaer 注意事项
- ✅ Python Mixin ≈ Java 接口 + 默认方法
- ⚠️ Python 支持真正的多重继承
- ⚠️ Mixin 应该是"无状态"的工具类
- 📌 记忆: "Mixin = 可组合的功能模块"

**相关文件:** `mixins.py`

---

## 总结

| 特性 | Python | Java |
|------|--------|------|
| 多态 | 鸭子类型 | 接口/继承 |
| 多重继承 | ✅ 支持 | ❌ 不支持 (接口可多实现) |
| Mixins | 多重继承 | 接口 + 默认方法 |

---

**制作时间:** 2025-11-25
**基于:** DeadSimplePython Chapter 13 示例代码
