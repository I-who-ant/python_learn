# .emacs.local 目录配置文件分析

## 概览

`~/.emacs.local/` 目录包含了 **9 个自定义 Emacs Major Mode 文件**,这些都是为小众编程语言或特定项目创建的语法高亮和编辑支持。

**来源:** Tsoding (Alexey Kutepov) 的 dotfiles,他是一位知名程序员/直播主,创建了许多自己的编程语言和工具。

---

## 文件清单

```
.emacs.local/
├── basm-mode.el         (2.8K) - BASM 汇编语言
├── c3-mode.el           (4.0K) - C3 语言
├── fasm-mode.el         (26K)  - FASM 汇编语言
├── jai-mode.el          (6.2K) - Jai 语言
├── noq-mode.el          (2.9K) - Noq 语言
├── nothings-mode.el     (846B) - Nothings 风格 C 代码
├── porth-mode.el        (2.7K) - Porth 语言
├── simpc-mode.el        (5.2K) - Simple C Mode
└── umka-mode.el         (6.0K) - Umka 语言
```

**总计:** ~57K 的自定义 major mode 代码

---

## 逐个文件分析

### 1. `basm-mode.el` - BASM 汇编语言模式

**项目地址:** https://github.com/tsoding/bm

**用途:** 为 BASM (一个简单的虚拟机汇编语言) 提供语法高亮和编辑支持

**语言特点:**
- 基于栈的虚拟机汇编
- 简单的指令集(push, pop, add, sub, jmp等)

**功能:**
- ✅ 语法高亮 (指令、标签、预处理器)
- ✅ 注释支持 (`;` 行注释)
- ✅ 自动模式关联 (`.basm`, `.hasm` 文件)

**关键指令示例:**
```basm
push 42        ; 压栈
dup            ; 复制栈顶
plusi          ; 整数加法
jmp label      ; 跳转
label:         ; 标签定义
```

**是否需要配置:**
- **如果你在学习/使用 Tsoding 的 BM 项目** → ✅ 需要
- **否则** → ❌ 不需要

---

### 2. `c3-mode.el` - C3 语言模式

**项目地址:** https://c3-lang.org/

**用途:** C 语言的现代化替代品,保持简洁但改进语法

**语言特点:**
- 基于 C 语言,但去除了历史包袱
- 更好的模块系统、泛型、错误处理
- 类似 Zig/Odin 的定位

**功能:**
- ✅ 语法高亮 (关键字、类型、预处理器)
- ✅ 自动缩进 (基于大括号)
- ✅ 注释支持 (`//` 和 `/* */`)
- ✅ 自动模式关联 (`.c3`, `.c3i` 文件)

**类型系统:**
```c3
// 整数类型
short, ushort, int, uint, long, ulong, int128, uint128

// 浮点类型
float16, float, double, float128

// 特殊类型
any, anyfault, typeid

// C 兼容类型
CInt, CLong, CDouble, ...
```

**关键字示例:**
```c3
fn main() -> int {
    int x = 42;
    if (x > 0) {
        return 0;
    }
}
```

**是否需要配置:**
- **如果你在学习 C3 语言** → ✅ 需要
- **否则** → ❌ 不需要

---

### 3. `fasm-mode.el` - FASM 汇编语言模式

**项目地址:** https://flatassembler.net/

**用途:** FASM (Flat Assembler) 是一个流行的 x86/x64 汇编器

**语言特点:**
- 强大的宏系统
- 自举(用自己编译自己)
- 广泛用于操作系统开发、逆向工程

**功能:**
- ✅ 完整的语法高亮
  - 数字字面量 (二进制、十六进制、十进制)
  - 数据类型 (byte, word, dword, qword)
  - 指令 (x86/x64 所有指令,包括 AVX/SSE)
  - 寄存器 (rax, rbx, xmm0, ymm0, 等)
  - 指令、预处理器
- ✅ 自动缩进 (基于标签和代码)
- ✅ 注释支持 (`;`)

**指令覆盖范围:**
- 基础指令: mov, add, sub, jmp, call, ret
- SSE/AVX: movaps, addps, vaddpd
- 特殊指令: cmpxchg16b, rdtsc, cpuid

**是否需要配置:**
- **如果你做汇编编程/逆向工程/操作系统开发** → ✅ 需要
- **否则** → ❌ 不需要

---

### 4. `jai-mode.el` - Jai 语言模式

**项目地址:** https://github.com/BSVino/JaiPrimer (非官方)

**用途:** Jonathan Blow 的 Jai 编程语言(游戏开发专用)

**语言特点:**
- 专为游戏开发设计
- 编译时代码执行
- 类型反射、元编程
- 目前还未公开发布

**功能:**
- ✅ 语法高亮
  - 关键字: if, while, for, struct, defer
  - 类型: int, u64, s32, float, string, bool
  - 内建函数: cast, size_of, type_info
  - 预处理器: `#run`, `#import`
  - 指令: `@`指令
- ✅ 基于 `js-indent-line` 的缩进
- ✅ 函数导航 (beginning/end-of-defun)
- ✅ imenu 支持 (快速跳转)
- ✅ 编译错误识别
- ✅ 注释支持 (`//` 和 `/* */`)

**语法示例:**
```jai
Vector3 :: struct {
    x: float;
    y: float;
    z: float;
}

main :: () {
    v: Vector3;
    v.x = 1.0;
    print("Hello, %!\n", v.x);
}
```

**是否需要配置:**
- **如果你在学习 Jai 或游戏开发** → ✅ 需要
- **否则** → ❌ 不需要

---

### 5. `noq-mode.el` - Noq 语言模式

**项目地址:** https://github.com/tsoding/noq

**用途:** Noq 是一个交互式的项重写系统(term rewriting system)

**语言特点:**
- 基于模式匹配和重写规则
- 用于符号计算和证明
- 交互式命令系统

**功能:**
- ✅ 语法高亮
  - 关键字: undo, quit, delete, load, save
  - Apply 策略: all, deep
  - 变量: 大写开头的标识符
  - Functor 名称
- ✅ 注释支持 (`#` Python 风格, `//` C 风格)

**语法示例:**
```noq
# 定义重写规则
add(X, 0) :: X
add(X, s(Y)) :: s(add(X, Y))

# 应用规则
all | add(s(s(0)), s(0))
```

**是否需要配置:**
- **如果你在学习符号计算/项重写系统** → ✅ 需要
- **否则** → ❌ 不需要

---

### 6. `nothings-mode.el` - Nothings 风格 C 模式

**用途:** 为 Sean Barrett (Nothings) 的单头文件 C 库提供极简支持

**相关项目:**
- stb_image.h (图片加载)
- stb_truetype.h (字体渲染)
- stb_sprintf.h (printf 实现)

**语言特点:**
- 单头文件 C 库风格
- 极简的预处理器高亮

**功能:**
- ✅ 预处理器高亮 (`#include`, `#define`)
- ✅ 注释支持 (`//`)

**是否需要配置:**
- **如果你在阅读/编写 stb 风格的单头文件库** → ✅ 需要
- **否则** → ❌ 不需要

---

### 7. `porth-mode.el` - Porth 语言模式

**项目地址:** https://github.com/tsoding/porth

**用途:** Porth 是一个用 Python 实现的 Forth 风格编程语言

**语言特点:**
- 基于栈
- 类 Forth 语法
- 编译到汇编

**功能:**
- ✅ 语法高亮
  - 关键字: if, else, while, do, proc, memory, const
  - 控制流: include, assert, return
  - 内存操作: peek, addr-of
- ✅ 注释支持 (`//` 和 `/* */`)

**语法示例:**
```porth
proc add5 int -> int do
    5 +
end

memory buffer 100 end

10 add5 print
```

**是否需要配置:**
- **如果你在学习 Forth 或栈式编程语言** → ✅ 需要
- **否则** → ❌ 不需要

---

### 8. `simpc-mode.el` - Simple C 模式

**用途:** 轻量级的 C/C++ 编辑模式,避免 cc-mode 的复杂性

**功能:**
- ✅ 完整的 C/C++ 语法高亮
  - 类型: char, int, long, void, bool, size_t, uint64_t
  - 关键字: if, for, while, return, struct, enum, typedef
  - C++ 关键字: class, template, namespace, constexpr
  - 预处理器: `#include`, `#define`, `#warn`, `#error`
- ✅ 智能缩进
  - 基于大括号
  - switch/case 特殊处理
  - 标签缩进
- ✅ 注释支持 (`//` 和 `/* */`)

**为什么不用内置的 c-mode?**
- c-mode 太复杂、太慢
- simpc-mode 简单、快速、够用

**是否需要配置:**
- **如果你觉得 c-mode 太慢,想要轻量级替代** → ✅ 可以考虑
- **否则使用内置 c-mode** → ❌ 不需要

---

### 9. `umka-mode.el` - Umka 语言模式

**项目地址:** https://github.com/rexim/umka (Tsoding 的另一个项目)

**用途:** Umka 是一个小型的静态类型脚本语言

**语言特点:**
- Go 风格的语法
- 静态类型
- 内置纤程(fiber)支持

**功能:**
- ✅ 语法高亮
  - 类型: str, void, int, uint, bool, real, fiber, any
  - 关键字: fn, if, for, struct, interface, import
  - 内建函数: len, cap, make, append, printf, sqrt
- ✅ 智能缩进 (类似 simpc-mode)
- ✅ 注释支持 (`//` 和 `/* */`)
- ✅ 自动模式关联 (`.um` 文件)

**语法示例:**
```umka
fn factorial(n: int): int {
    if n <= 1 {
        return 1
    }
    return n * factorial(n - 1)
}

fn main() {
    printf("5! = %d\n", factorial(5))
}
```

**是否需要配置:**
- **如果你在学习 Umka 语言** → ✅ 需要
- **否则** → ❌ 不需要

---

## 统一功能对比表

| Mode | 语言类型 | 项目状态 | 语法高亮 | 自动缩进 | 优先级 |
|------|---------|---------|---------|---------|--------|
| **basm-mode** | 汇编(虚拟机) | Tsoding 项目 | ✅ | ❌ | ⭐ |
| **c3-mode** | 系统级 | 活跃开发 | ✅ | ✅ | ⭐⭐⭐ |
| **fasm-mode** | x86 汇编 | 成熟稳定 | ✅✅✅ | ✅ | ⭐⭐⭐⭐ |
| **jai-mode** | 游戏开发 | 未公开 | ✅ | ✅ | ⭐⭐ |
| **noq-mode** | 项重写 | Tsoding 项目 | ✅ | ❌ | ⭐ |
| **nothings-mode** | C 库 | 工具性 | ✅ | ❌ | ⭐ |
| **porth-mode** | Forth风格 | Tsoding 项目 | ✅ | ❌ | ⭐ |
| **simpc-mode** | C/C++ | 轻量替代 | ✅ | ✅ | ⭐⭐⭐⭐ |
| **umka-mode** | 脚本语言 | Tsoding 项目 | ✅ | ✅ | ⭐⭐ |

---

## 推荐配置策略

### 方案 1: 全部加载(学习/探索用)

如果你想全面学习这些语言或探索 Tsoding 的项目:

```elisp
;; 在 ~/.emacs 中添加
(add-to-list 'load-path "~/.emacs.local/")

;; 加载所有模式
(require 'basm-mode)
(require 'c3-mode)
(require 'fasm-mode)
(require 'jai-mode)
(require 'noq-mode)
(require 'nothings-mode)
(require 'porth-mode)
(require 'simpc-mode)
(require 'umka-mode)
```

**优点:** 开箱即用,随时可以打开相关文件
**缺点:** 启动时加载不必要的模式

---

### 方案 2: 按需加载(推荐)

只加载你实际会用到的:

```elisp
;; 在 ~/.emacs 中添加
(add-to-list 'load-path "~/.emacs.local/")

;; 汇编开发者
(when (or (executable-find "fasm")
          (file-exists-p "~/projects/asm"))
  (require 'fasm-mode))

;; C3 语言学习者
(when (executable-find "c3c")
  (require 'c3-mode))

;; C/C++ 开发(想要轻量级模式)
(require 'simpc-mode)
;; 可选: 让 simpc-mode 替代 c-mode
;; (add-to-list 'auto-mode-alist '("\\.c\\'" . simpc-mode))
;; (add-to-list 'auto-mode-alist '("\\.h\\'" . simpc-mode))
```

---

### 方案 3: 自动加载(最优雅)

使用 Emacs 的 autoload 机制:

```elisp
;; 在 ~/.emacs 中添加
(add-to-list 'load-path "~/.emacs.local/")

;; 所有模式文件已经包含 ;;;###autoload 标记
;; 只需更新 autoloads
(let ((generated-autoload-file "~/.emacs.local/autoloads.el"))
  (update-directory-autoloads "~/.emacs.local/"))

;; 加载 autoloads
(load "~/.emacs.local/autoloads.el")
```

**优点:** 只在打开相应文件时才加载模式,不影响启动速度
**缺点:** 需要额外生成 autoloads.el

---

## 实际使用场景

### 场景 1: 你是汇编/底层开发者

```elisp
(add-to-list 'load-path "~/.emacs.local/")
(require 'fasm-mode)
(require 'basm-mode)
```

**用途:**
- 编写 x86/x64 汇编代码
- 学习虚拟机实现
- 逆向工程

---

### 场景 2: 你在学习新兴系统语言

```elisp
(add-to-list 'load-path "~/.emacs.local/")
(require 'c3-mode)
(require 'jai-mode)
```

**用途:**
- 探索 C 语言的现代替代品
- 游戏开发

---

### 场景 3: 你想要轻量级 C/C++ 模式

```elisp
(add-to-list 'load-path "~/.emacs.local/")
(require 'simpc-mode)

;; 可选: 替代默认的 c-mode
(add-to-list 'auto-mode-alist '("\\.c\\'" . simpc-mode))
(add-to-list 'auto-mode-alist '("\\.h\\'" . simpc-mode))
```

**优点:**
- 启动更快
- 语法高亮够用
- 智能缩进

---

### 场景 4: 你在跟随 Tsoding 的教程

```elisp
(add-to-list 'load-path "~/.emacs.local/")

;; 加载 Tsoding 创建的所有语言
(require 'basm-mode)
(require 'porth-mode)
(require 'noq-mode)
(require 'umka-mode)
```

---

## 常见问题

### Q1: 这些模式会影响 Emacs 启动速度吗?

**A:** 取决于加载方式

```
全部 require     → 启动时间 +0.1~0.2 秒
按需 require     → 启动时间 +0.01 秒
autoload 方式    → 启动时间 +0 秒(第一次打开文件时加载)
```

---

### Q2: 我不用这些语言,需要配置吗?

**A:** **不需要**

如果你只使用 Python/JavaScript/C++ 等主流语言:
- 不需要添加 `load-path`
- 不需要 require 这些模式
- 保持 `~/.emacs` 简洁即可

---

### Q3: 如果我只想尝试一两个语言呢?

**A:** 按需加载

```elisp
;; 只想试试 C3
(add-to-list 'load-path "~/.emacs.local/")
(require 'c3-mode)

;; 当你不需要时,注释掉或删除
;; (require 'c3-mode)
```

---

### Q4: simpc-mode 和内置 c-mode 有什么区别?

**A:** 对比如下

| 特性 | c-mode (内置) | simpc-mode |
|------|--------------|------------|
| 语法高亮 | ✅ 非常完整 | ✅ 足够用 |
| 自动缩进 | ✅ 非常智能 | ✅ 基础智能 |
| 启动速度 | ❌ 慢 | ✅ 快 |
| 配置复杂度 | ❌ 高 | ✅ 低 |
| LSP 支持 | ✅ 完美 | ✅ 可以配合 |

**推荐:**
- **大型 C++ 项目** → 用 c-mode + LSP
- **小型 C 项目/快速编辑** → 用 simpc-mode

---

### Q5: 这些模式支持 LSP 吗?

**A:** 部分支持

```elisp
;; C3-mode 可以配合 LSP
(use-package lsp-mode
  :hook (c3-mode . lsp))

;; simpc-mode 可以配合 clangd
(use-package lsp-mode
  :hook (simpc-mode . lsp))
```

但大多数小众语言没有 LSP 服务器。

---

## 我的推荐

### 如果你是...

#### 🎓 **学习者/探索者**
```elisp
;; 全部加载,探索不同语言
(add-to-list 'load-path "~/.emacs.local/")
(mapc 'require '(c3-mode jai-mode porth-mode umka-mode))
```

---

#### 💻 **实用主义者**
```elisp
;; 只加载你确定会用的
(add-to-list 'load-path "~/.emacs.local/")
(require 'simpc-mode)  ; C/C++ 轻量级替代
```

---

#### ⚡ **性能优先者**
```elisp
;; 使用 autoload,不影响启动
(add-to-list 'load-path "~/.emacs.local/")
;; 文件已有 autoload 标记,打开相应文件时自动加载
```

---

#### 🚫 **主流语言用户**
```
不需要配置任何东西!
```

---

## 总结

### 核心要点

1. **9 个模式覆盖 9 种小众语言**
   - 汇编: BASM, FASM
   - 系统语言: C3, Jai
   - 脚本语言: Umka
   - 栈式语言: Porth
   - 工具: Noq, Nothings, Simple C

2. **大部分是 Tsoding 的个人项目**
   - BASM, Porth, Noq, Umka 都是他创建的
   - FASM, C3, Jai 是他在用的语言

3. **配置策略**
   - **不确定** → 不配置(保持简洁)
   - **想探索** → 全部加载
   - **知道要用** → 按需加载
   - **追求性能** → autoload

4. **最实用的两个**
   - `simpc-mode` - C/C++ 轻量级替代
   - `fasm-mode` - 汇编开发(如果你做底层)

---

## 快速决策树

```
你是否在学习这些语言?
├── 是 → 加载对应的 mode
│   ├── C3 → require 'c3-mode
│   ├── 汇编 → require 'fasm-mode
│   └── Tsoding 项目 → require 对应 mode
│
└── 否 → 是否想要轻量级 C mode?
    ├── 是 → require 'simpc-mode
    └── 否 → 不需要配置任何东西!
```

---

**建议:** 先不配置,等你真正需要编辑这些语言的文件时再配置。保持配置文件简洁是最佳实践!
