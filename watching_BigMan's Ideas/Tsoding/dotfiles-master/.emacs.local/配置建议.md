# .emacs.local 配置建议

## 快速决策

### ❓ 我需要配置吗?

```
你是否在使用这些小众语言?
├── 否 → ❌ 不需要配置(保持简洁)
├── 不确定 → ❌ 暂时不配置
└── 是 → ✅ 继续往下看
```

---

## 配置方案

### 方案 1: 完全不配置 (推荐新手)

**适用:** 主要使用 Python/JavaScript/C++ 等主流语言

```elisp
# 不需要在 ~/.emacs 中添加任何配置
# 保持简洁即可
```

**优点:**
- ✅ 配置文件简洁
- ✅ 启动速度快
- ✅ 不加载不需要的东西

---

### 方案 2: 按需加载 (推荐)

**适用:** 明确知道要用哪些语言

```elisp
;; 在 ~/.emacs 中添加
(add-to-list 'load-path "~/.emacs.local/")

;; 只加载你需要的模式
(require 'c3-mode)      ; C3 语言
(require 'fasm-mode)    ; FASM 汇编
(require 'simpc-mode)   ; 轻量级 C/C++
```

**优点:**
- ✅ 明确控制加载什么
- ✅ 启动速度影响小
- ✅ 按需启用

---

### 方案 3: 自动加载 (推荐高级用户)

**适用:** 想要零启动开销,按需加载

```elisp
;; 在 ~/.emacs 中添加
(add-to-list 'load-path "~/.emacs.local/")

;; 所有模式文件已包含 ;;;###autoload 标记
;; 打开相应扩展名文件时自动加载模式
;; 无需 require,零启动开销
```

**优点:**
- ✅ 启动速度不受影响 (0 秒开销)
- ✅ 打开文件时自动激活对应模式
- ✅ 最优雅的方案

---

### 方案 4: 全部加载 (仅限学习/探索)

**适用:** 想全面探索这些语言

```elisp
;; 在 ~/.emacs 中添加
(add-to-list 'load-path "~/.emacs.local/")

;; 加载所有模式
(require 'basm-mode)
(require 'c3-mode)
(require 'fasm-mode)
(require 'jai-mode)
(require 'noq-mode)
(require 'nothings-mode)
(require 'porth-mode)
(require 'simpc-mode)
(require 'umka-mode)
```

**缺点:**
- ❌ 启动时间 +0.1~0.2 秒
- ❌ 加载不必要的模式

---

## 使用场景推荐

### 场景 1: 汇编/底层开发

```elisp
(add-to-list 'load-path "~/.emacs.local/")
(require 'fasm-mode)    ; x86/x64 汇编
(require 'basm-mode)    ; 虚拟机汇编 (可选)
```

**用途:**
- 编写汇编代码
- 逆向工程
- 操作系统开发

---

### 场景 2: 学习新兴系统语言

```elisp
(add-to-list 'load-path "~/.emacs.local/")
(require 'c3-mode)      ; C 的现代替代品
(require 'jai-mode)     ; 游戏开发语言 (可选)
```

**用途:**
- 探索现代系统编程语言
- 学习语言设计

---

### 场景 3: 想要轻量级 C/C++ 模式

```elisp
(add-to-list 'load-path "~/.emacs.local/")
(require 'simpc-mode)

;; 可选: 让 simpc-mode 替代默认 c-mode
(add-to-list 'auto-mode-alist '("\\.c\\'" . simpc-mode))
(add-to-list 'auto-mode-alist '("\\.h\\'" . simpc-mode))
```

**优点:**
- 启动比 c-mode 快
- 语法高亮够用
- 智能缩进

**对比:**
| 特性 | c-mode | simpc-mode |
|------|--------|-----------|
| 语法高亮 | ✅ 完整 | ✅ 足够 |
| 启动速度 | ❌ 慢 | ✅ 快 |
| 配置复杂度 | ❌ 高 | ✅ 低 |

---

### 场景 4: 跟随 Tsoding 的项目

```elisp
(add-to-list 'load-path "~/.emacs.local/")

;; Tsoding 创建的语言
(require 'basm-mode)    ; BM 虚拟机
(require 'porth-mode)   ; Porth 语言
(require 'noq-mode)     ; Noq 项重写系统
(require 'umka-mode)    ; Umka 脚本语言
```

---

## 具体配置步骤

### 步骤 1: 复制文件到 ~/.emacs.local/

```bash
# 如果还没有复制
mkdir -p ~/.emacs.local
cp -r "watching_BigMan's Ideas/Tsoding/dotfiles-master/.emacs.local/"* \
      ~/.emacs.local/
```

---

### 步骤 2: 在 ~/.emacs 中添加配置

根据上面选择的方案,添加相应配置到 `~/.emacs` 文件中。

**示例 - 按需加载 C3 和 FASM:**

```elisp
;; 添加到 ~/.emacs 的合适位置 (比如文件末尾)

;; ========================================
;; 自定义语言模式
;; ========================================
(add-to-list 'load-path "~/.emacs.local/")

;; 加载需要的模式
(require 'c3-mode)      ; C3 语言支持
(require 'fasm-mode)    ; FASM 汇编支持
```

---

### 步骤 3: 重启 Emacs 或重新加载配置

```elisp
;; 方法1: 重启 Emacs

;; 方法2: 在 Emacs 中执行
M-x eval-buffer

;; 方法3: 在 Emacs 中执行
M-x load-file RET ~/.emacs RET
```

---

### 步骤 4: 测试是否生效

```bash
# 创建测试文件
echo 'fn main() { }' > test.c3

# 在 Emacs 中打开
emacs test.c3

# 应该看到语法高亮,并在 mode-line 显示 "C3"
```

---

## 各模式文件扩展名

当你打开以下扩展名的文件时,对应的模式会自动激活:

| 扩展名 | 模式 | 语言 |
|-------|------|------|
| `.basm`, `.hasm` | basm-mode | BASM 汇编 |
| `.c3`, `.c3i` | c3-mode | C3 语言 |
| `.asm`, `.inc` | fasm-mode | FASM 汇编 |
| `.jai` | jai-mode | Jai 语言 |
| `.noq` | noq-mode | Noq |
| `.porth` | porth-mode | Porth |
| `.um` | umka-mode | Umka |
| `.c`, `.h` | simpc-mode* | C/C++ (需手动配置) |

*注: simpc-mode 需要手动配置 `auto-mode-alist` 才能替代 c-mode

---

## 常见问题

### Q1: 配置后 Emacs 启动变慢了?

**A:** 检查是否加载了太多不需要的模式

```elisp
;; 不推荐: 加载所有
(require 'basm-mode)
(require 'c3-mode)
(require 'fasm-mode)
...

;; 推荐: 只加载需要的
(require 'c3-mode)
```

或者使用自动加载(零开销):
```elisp
;; 只添加 load-path,不 require
(add-to-list 'load-path "~/.emacs.local/")
;; 打开文件时自动加载
```

---

### Q2: 如何卸载某个模式?

**A:** 从 ~/.emacs 中删除或注释掉对应行

```elisp
;; 注释掉不需要的
;; (require 'basm-mode)

;; 或直接删除
```

---

### Q3: 我只想试试,不想永久配置?

**A:** 临时加载模式

```elisp
;; 在 Emacs 中执行 (M-x eval-expression)
(add-to-list 'load-path "~/.emacs.local/")
(require 'c3-mode)

;; 重启 Emacs 后不会保留
```

---

### Q4: simpc-mode 和 c-mode 哪个好?

**A:** 取决于需求

**使用 c-mode (内置) 如果:**
- ✅ 大型 C++ 项目
- ✅ 需要复杂的缩进控制
- ✅ 配合 LSP 使用

**使用 simpc-mode 如果:**
- ✅ 小型 C 项目
- ✅ 追求启动速度
- ✅ 喜欢简单配置

**可以共存:**
```elisp
;; 默认用 c-mode,特定文件用 simpc-mode
(require 'simpc-mode)
;; C 文件继续用 c-mode,不覆盖 auto-mode-alist
```

---

### Q5: 这些模式支持 LSP 吗?

**A:** 部分支持

```elisp
;; C3-mode 可以配合 LSP (如果有 c3-lsp)
(use-package lsp-mode
  :hook (c3-mode . lsp))

;; simpc-mode 可以配合 clangd
(use-package lsp-mode
  :hook (simpc-mode . lsp))
```

大多数小众语言没有 LSP 服务器。

---

## 推荐配置示例

### 示例 1: 最小配置 (推荐)

```elisp
;; ~/.emacs

;; 只添加 load-path,使用自动加载
(add-to-list 'load-path "~/.emacs.local/")

;; 不需要 require,打开文件时自动激活
;; 零启动开销!
```

---

### 示例 2: 实用主义者

```elisp
;; ~/.emacs

;; 添加 load-path
(add-to-list 'load-path "~/.emacs.local/")

;; 只加载实用的模式
(require 'simpc-mode)   ; 轻量级 C mode
(require 'c3-mode)      ; C3 语言

;; 让 simpc-mode 处理 .c 和 .h 文件
(add-to-list 'auto-mode-alist '("\\.c\\'" . simpc-mode))
(add-to-list 'auto-mode-alist '("\\.h\\'" . simpc-mode))
```

---

### 示例 3: 汇编开发者

```elisp
;; ~/.emacs

(add-to-list 'load-path "~/.emacs.local/")

;; 汇编相关模式
(require 'fasm-mode)    ; FASM 汇编
(require 'basm-mode)    ; BM 虚拟机汇编

;; 可选: 配置编译命令
(add-hook 'fasm-mode-hook
  (lambda ()
    (setq compile-command
          (format "fasm %s" (buffer-file-name)))))
```

---

### 示例 4: 语言学习者

```elisp
;; ~/.emacs

(add-to-list 'load-path "~/.emacs.local/")

;; 探索不同的编程语言
(require 'c3-mode)      ; C3: 现代 C
(require 'jai-mode)     ; Jai: 游戏开发
(require 'umka-mode)    ; Umka: 小型脚本语言
(require 'porth-mode)   ; Porth: Forth 风格
```

---

## 性能对比

| 配置方式 | 启动时间影响 | 内存占用 | 推荐度 |
|---------|------------|---------|--------|
| 不配置 | 0 秒 | 0 MB | ⭐⭐⭐⭐⭐ |
| 自动加载 | ~0 秒 | ~0 MB | ⭐⭐⭐⭐⭐ |
| 按需加载 (1-2个) | +0.01 秒 | +1 MB | ⭐⭐⭐⭐ |
| 按需加载 (5个) | +0.05 秒 | +3 MB | ⭐⭐⭐ |
| 全部加载 (9个) | +0.1~0.2 秒 | +5 MB | ⭐⭐ |

---

## 决策树

```
                    开始
                     ↓
         使用这些小众语言吗?
                ↙        ↘
              是          否
              ↓           ↓
    知道具体用哪些?   不需要配置!
        ↙        ↘      (结束)
      是          否
      ↓           ↓
  按需加载    自动加载
   (方案2)    (方案3)
      ↓           ↓
   完成!       完成!
```

---

## 总结

### ✅ 最佳实践

1. **新手/不确定** → 不配置,保持简洁
2. **明确需求** → 按需加载 (方案 2)
3. **追求性能** → 自动加载 (方案 3)
4. **学习探索** → 全部加载 (方案 4)

### 🎯 我的推荐

**大多数人:**
```elisp
# 暂时不配置
# 等真正需要时再添加
```

**如果想尝试 C3 语言:**
```elisp
(add-to-list 'load-path "~/.emacs.local/")
(require 'c3-mode)
```

**如果想要快速的 C mode:**
```elisp
(add-to-list 'load-path "~/.emacs.local/")
(require 'simpc-mode)
(add-to-list 'auto-mode-alist '("\\.c\\'" . simpc-mode))
```

---

## 相关文档

- **详细分析:** `watching_BigMan's Ideas/Emacs/.emacs.local配置分析.md`
- **Tsoding dotfiles:** `watching_BigMan's Ideas/Tsoding/dotfiles-master/`
- **各模式源码:** `~/.emacs.local/*.el`

---

**记住:** 保持配置简洁是美德。不要加载你不需要的东西! 🎯
